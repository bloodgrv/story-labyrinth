# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Baked local embedding model weights (server/scripts/prefetchEmbeddingModel.mjs) — large
# downloaded ONNX weights, not source. See docs/Local_Embeddings_Design.md.
.embedding-model-cache

# Windows portable build output (scripts/build-portable.mjs) — generated, not source.
.portable-build-cache
portable-build
Story-Labyrinth-portable-win-x64.zip
Story-Labyrinth-portable-win-x64-update.zip

# Local Hermes planning-agent output — not source.
.hermes/

# App-generated manuscript backups and dev SQLite DBs — not source.
data/manuscript-backups/
*.db

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.development
.env.production
.env.test

.cursorrules
.cursor/

# Windows reserved device names
NUL
