# Story Labyrinth — Current Backlog

**Last updated:** 2026-08-09 (T5 Lore Sheet + Sync Loop — FS0+FS1+FS2+FS3+FS4 shipped, `docs/Lore_Sheet_And_Sync_Design.md`: schema, sheet-first editor UI (Natural View retired), deterministic reverse-compile + optional Improve-with-AI, "Sync structured fields" (hybrid deterministic+LLM parse into Codex tray proposals), and now a WB `sheet-proposal` fence (any anchored WB chat can draft/expand the sheet) + "Accept & Sync" chain; also scoped (not built) a new **T9** inline sub-span "Rework in chat" for the Lore Sheet — `docs/Lore_Sheet_Inline_Rework_Design.md`, P3, not promoted; and a new **T10** chat-chrome icon-rail/drawer declutter for the WB/Outline/Notes/Research/Brainstorm chats — `docs/Chat_Chrome_Declutter_Design.md`, P3, not promoted, kept separate from T8 per explicit user direction; prior: T7 Notes desk org + browse UI **shipped in full** — `docs/Notes_Org_Browse_Design.md`, NO0–NO6: sticky Browse + closable note tabs, Cards\|List, cosmetic folders extending the B9 engine to `kind: notes`, thin tags, smart piles All\|Unfiled\|Pinned\|Armed\|Recent, title+body search; prior: Story Timeline TL9-TL12 shipped in full — TL0-TL12; SL5 shipped; SL0–SL3 + BT0–BT4; dep pack 3 ROI freeze)  
**Purpose:** Single source of truth for **what’s left**, after implementation order got scrambled relative to the original Phase 0 list.  
**Canonical live status also mirrored in:** `CLAUDE.md` (architecture + high-level “done” notes) and `DECISIONS.md` (load-bearing how/why).  
**This file wins** when those conflict on *priority of remaining work*.

---

## How to use this

1. Pick the next **P0** item unless the user explicitly redirects.
2. Do not re-implement items in **Done**.
3. Phase C / G1.5 items are **à la carte** — not a mandatory package.
4. After finishing an item: move it to Done (or mark partial), note the date, and record load-bearing choices in `DECISIONS.md`.

---

## Done (do not re-plan as foundation)

| Area | Notes |
|------|--------|
| Fork + Docker + basic login | |
| Character Codex (concrete state only) + propose/approve | |
| Codex Project Saves layer (snapshot timeline + restore) | Lorebook entry History UI |
| Per-feature AI endpoints + Grok routing editor | |
| Vector RAG (sqlite-vec + FTS5 hybrid) + index-on-write | Lorebook create/update, chapter save, Codex approve reindex |
| RAG Scanner **backend** + job-wrapped scans | `ragScans` / `ragScanIssues`; dual-write via `agentJobs` |
| World-building / research / editor chats + entry/chapter anchors | |
| Editor MultiView (panes, tabs, float, persistence) | |
| Story outlining + character arcs + AI outline suggestions | |
| Concrete Beats (manual, AI suggest, auto-tag, guide) | |
| Deep Writing / Focus sessions | |
| Humanizer | |
| TTS (Speechify) | |
| Lorebook images (upload + AI generate) | |
| Multi-format lorebook document import | Fixed 2026-07-17: PDF page-break marker leak, description-stripping prompt, PDF + DOCX embedded-image extraction (largest image by area). Added native `canvas` dependency + Dockerfile system libs, per explicit user confirmation — see `DECISIONS.md` |
| Baseline novel export (md/html/text/epub/pdf paths) | |
| Visible model thinking tags (parse + ThinkingBlock) | **Partial product** — not full multi-agent reasoning UI |
| Agent Framework Phase A (jobs, runner, reconcile, prune, scan-as-job) | Settings `RecentJobsCard` |
| Agent Framework Phase B (project memory, pending approve, distill job) | Memory tool; distill manual enqueue only |
| Lorebook Relationship Graph Phase G1 | `storyGraphEdges` SoT, React Flow Relationships tool, ego + full view |
| Lorebook entry "Natural View" (prose character profile toggle) | `NaturalEntryView.tsx`; same form data as raw fields, tags hidden; Codex auto-compile (P0.3/C5) NOT included |
| Theme-tint fix: `MainContent.tsx` `toolTints` no longer breaks under custom (non-`.dark`) themes | Was causing a visible light "block" per tool pane on Midnight/Forest/Sand/Graphite |
| RAG Scanner frontend (P0.1) | Editor right-rail drawer (chapter-scoped) + "Scanner" sidebar tool (story-wide, scan history, status tabs), both triggering via `POST /api/agent/jobs` (owner-only). See `DECISIONS.md` "RAG Scanner Frontend (P0.1)" |
| Chapter alternate-version tabs (P0.2, redefined — see note below) | Flat, chat-branching-style draft tabs next to the main chapter (`ChapterVersionsPanel.tsx`), created via AI regenerate or manual duplicate, independently editable, one-way "Compile to Main" action, optional side-by-side compare toggle. See `DECISIONS.md` "Story-Layer Chapter Versioning (P0.2)" |
| Chapter content undo/restore (P0.2b) | Linear history for the main chapter's own content (`chapterSnapshots` table, mirrors Codex snapshot/restore) — throttled ~15min auto-checkpoints, manual named saves, restore via a new "History" editor drawer. Compile (P0.2) now takes a safety checkpoint first too. See `DECISIONS.md` "Chapter Content Undo/Restore (P0.2b)" |
| Notes/Outline ↔ Chat bridge, "Core Bridge" scope (P0.3 N0-N4, O1-O4) | Double-gate opt-in (`includeInAi` per note/outline item + `includeNotes`/`includeOutline` per chat) — schema, RAG `note`/`outline_item` entity types + reconcile_index, per-item toggle UI, story export/import round-trip, `chatContextService` non-canon packets surfaced in World-Building/Research/Editor(-excluded)/Brainstorm chats. N5/N6 (save-as-note, note-proposal) NOT included. See `DECISIONS.md` "Notes/Outline ↔ Chat Bridge (P0.3, Core Bridge scope)" |
| Project Memory chat toggle (P0.3 C1) | `aiChats.includeMemory` toggle + `resolveMemories` in `chatContextService.ts`, surfaced in the same chat UI as the Notes/Outline toggles. No per-item flag needed — every active memory is already index-eligible via Phase B's approve step. See `DECISIONS.md` "Project Memory Chat Toggle (C1)" |
| Editor Selection Rework + Codex Proposal Tray (P0.4 R0-R3) | "Rework in chat" replaces the floating toolbar's one-shot Expand/Rewrite/Shorten as the primary path — highlight → bound Editor chat with before/selection/after context → Accept replaces only that selection. Codex proposals for Editor(/Outline) chats moved to a tray under the chat list with Approve/Reject/Edit. Fixed two real pre-existing bugs found along the way (`activeChapterEditorStore` single-slot bug, chat-list cache invalidation). See `DECISIONS.md` "Editor Selection Rework + Codex Proposal Tray (P0.4 R0-R3)" |
| Lorebook Rework → WB, Dedicated Outline Chat, Outline Row Rework, WB Handoff (P0.4 R4/R5/R7/R8) | Description-field sub-span "Rework in chat" bound to the entry's WB chat (Accept = existing codex-proposal approve flow, no new apply path); WB Codex proposals unified onto the same tray Editor uses (Approve/Edit/Reject). Outline gets a real dedicated `chatType`/`promptType: "outline"` (R7's split, done here since R5 needed it) — own chat list/rail, always-on full outline tree + written-chapter-summaries context, new `outline-proposal`/`lore-suggestion` fences, retired bulk "Generate with AI" button (`outlineGenerator.ts` deleted). Outline rows get whole-field "Rework in chat" too; lore suggestions hand off to Lorebook via the same same-tab `StoryContext` pattern the Relationships graph's "Open entry" already used. Structured-Codex-field (wardrobe/wounds/items) rework and R6 (auto-accept toggle UI) explicitly cut/deferred. See `DECISIONS.md` "P0.4 R4/R5/R7/R8" |
| Brainstorm Hub — migrate to shared stack, Guided Setup, depth-adaptive propose, durable tray (P0.4 B0-B4) | Brainstorm moved off its own parallel chat implementation onto the shared `aiChats`/`chatContextService`/`ChatInterface` stack, own `chatType: "brainstorm"`. Composer blurb + Guided Setup button + Light/Standard/Grill-me style dropdown (confirmed prompt-driven with user, not a tracked interview state machine) + empty-story "Start in Brainstorm" CTA. Opt-in toggles: Notes/Outline/Memory (existing) plus new Lorebook/Chapter Summaries (Brainstorm is the first chat type where lorebook search defaults OFF). Two new fences — `overview-proposal` (synopsis/overview note/opt-in memory, one discriminated fence) and `handoff-packet` (Outline/WB/Notes/Research, multiple per reply) — persisted immediately on parse as durable `brainstormChecklist` rows (new table, genuinely different status lifecycle than `codexPendingChanges`: Open/Send/Accept perform the real write but stay Active, only "Mark done" moves to Done). WB handoff reuses the existing `pendingLorebookSeed` mechanism unchanged; Outline/Research reuse a new generalized `StoryContext.pendingChatComposerSeed` + `ChatInterface`'s new `initialComposerText` prop; Notes handoff creates directly (no chat rail exists yet). Separate `brainstormSlots` table backs a fixed 5-slot known/unknown setup checklist. Found and fixed a real pre-existing bug along the way: `POST /api/notes` never set `updatedAt` (NOT NULL column), silently 500ing every note-creation path in the app, not just this new one. B5 (WB/Outline domain playbooks) and the S/K/R6 tracks stay explicitly out of scope. See `DECISIONS.md` "P0.4 B0-B4" |

Design docs that still say “not implemented” in their headers may be stale for A/B/G1 — trust this file + `CLAUDE.md` over those headers until they are refreshed.

---

## Recommended build order (from here)

```
1. Continuity glue (memory ↔ chat/scan)    ← P0/P1
2. Graph G1.5 / Agent C only if daily pain ← P1
3. Bugfix pass                           ← P2 (anytime)
4. Nice-to-haves                           ← P3
```

---

## P0 — High leverage / incomplete core claims

### P0.1 — RAG Scanner frontend — ✅ Done (2026-07-18)

Editor right-rail drawer (`EditorToolsPanel.tsx`'s `"ragScanner"` drawer, chapter-scoped) and a story-wide "Scanner" sidebar tool (`RagScannerPanel.tsx`) both shipped — trigger via `POST /api/agent/jobs` (owner-only, per user decision), progress via job polling, issues list with Open/Resolved/Dismissed tabs bound to `ragScanIssues`. See `DECISIONS.md` "RAG Scanner Frontend (P0.1) — Load-Bearing Decisions".

**Inline highlighting of flagged spans — done (2026-07-28).** The one piece of the original `docs/2026-06-26_RAG_Scanner_Integration.md` design that never shipped in the P0.1 pass above. New ephemeral `RagIssueMarkNode` (`src/components/story-editor/nodes/RagIssueMarkNode.ts`, mirrors `GrammarMarkNode`'s never-persisted pattern) wraps `open`-issue evidence excerpts on demand (chapter mount / issue-list change, not per-keystroke) via exact substring match (`$findTextNodeRange`, reused unchanged from `beatTextSearch.ts`) — a paraphrased or since-edited excerpt just silently doesn't highlight, no fuzzy matching, no stale indicator (confirmed with user). Click opens `RagIssuePopover.tsx` (mirrors `GrammarIssuePopover.tsx`) with severity/description/suggested fix/evidence + Resolve/Dismiss, unwrapping the mark immediately client-side rather than waiting on the query-invalidation round-trip. Strip-before-save generalized from `stripGrammarMarks.ts` into `stripEphemeralMarks.ts` (now handles both `grammar-mark` and `rag-issue-mark` types). Still open if revisited: full dual-write retirement of `ragScans` (unrelated, tracked separately under P1.1 below), auto-chaining `distill_memory`. Live-verified in the Browser pane against the real dev DB (seeded via a disposable script, cleaned up after): exact-match excerpt highlighted with severity-colored underline, paraphrased excerpt correctly produced no highlight, click→popover→Resolve unwrapped the highlight immediately and persisted server-side, mark survived an unrelated edit elsewhere in the chapter, saved chapter JSON confirmed free of `rag-issue-mark` nodes. `npm run build` clean.

---

### P0.2 — Chapter alternate-version tabs — ✅ Done (2026-07-18), but read the note below

**This shipped a different feature than this item originally described**, per direct user correction mid-build: not linear chapter-content history/undo, but chat-branching-style alternate drafts — flat tabs next to the main chapter (`ChapterVersionsPanel.tsx`), created via AI regenerate (`GenerateVersionDialog.tsx`, new `chapter_version` feature endpoint) or manual duplicate, each independently editable (`VersionEditor.tsx`, its own autosave), with an optional side-by-side compare toggle and a one-way "Compile to Main" action. `chapterVersions` table, `server/routes/chapters.ts`'s `/versions` sub-routes. See `DECISIONS.md` "Story-Layer Chapter Versioning (P0.2) — Load-Bearing Decisions" for the full scoping trail and load-bearing choices.

**The original problem this item was written to solve was split out and is now done separately as P0.2b below** (chapters still had zero linear undo history and a fully destructive autosave when this item shipped — nothing about the alternate-version-tabs feature itself touched that).

**Refs:** `CLAUDE.md` Project Saves; `DECISIONS.md` "Project Saves — Phase 1 (Codex Layer…)" and "Story-Layer Chapter Versioning (P0.2)"

---

### P0.2b — Chapter content undo/restore — ✅ Done (2026-07-19)

Linear undo history for the main chapter's own content, mirroring the Codex `codexSnapshots`/restore precedent for real this time. New `chapterSnapshots` table (forms a restore chain, unlike P0.2's flat `chapterVersions`). Server-side throttled `'auto'` checkpoints (~15min per chapter, computed inside the chapter content `PUT` route) plus manual named saves and restore, surfaced via a new "History" drawer in `EditorToolsPanel.tsx`. Compile (P0.2) and Restore both now take an unconditional safety checkpoint before their one-way overwrite, closing the "no backup" gap P0.2 shipped with. See `DECISIONS.md` "Chapter Content Undo/Restore (P0.2b) — Load-Bearing Decisions" — including two real bugs caught live before shipping: a `createdAt` tie-break ordering bug, and a race condition where Restore/Compile could silently undo themselves seconds later via a stale-cache reload.

**Refs:** `CLAUDE.md` Project Saves; `DECISIONS.md` "Project Saves — Phase 1 (Codex Layer…)" and "Chapter Content Undo/Restore (P0.2b)"

---

### P0.3 — Continuity glue (memory + scanner + writing loop + Notes/Outline bridges)

**Status:** ✅ Fully done (2026-07-20). Notes/Outline bridge "Core Bridge" scope (N0-N4, O1-O4) — done. C1 (project memory chat toggle) — done. N5 (save message/selection as note), N6 (AI note-proposal accept/reject card), C2 ("suggest memories from this scan" button), C3 (scanner reads active Project Memory for contradiction checks, per-scan opt-in), C4 (per-story unattended scan schedule, default off), C5 (Codex auto-compile from manuscript via a new manual-trigger job + entry-scoped pending-changes review panel) — all done, same session. See `DECISIONS.md`'s "P0.3 Remaining Slices (N5/N6, C2-C5) — Load-Bearing Decisions".  
**Why:** Project memory, graph, scanner, Notes, and Outline don’t yet form a daily continuity loop. Models default to lorebook+chapter RAG only; Notes/Outline are human silos.

**N0-N4 + O1-O4 shipped 2026-07-20** — see `DECISIONS.md` "Notes/Outline ↔ Chat Bridge (P0.3, Core Bridge scope)" for the full load-bearing trail, including two real pre-existing bugs found (and one fixed) during verification: story export/import previously crashed on any lorebook entry with a non-null `updatedAt` (fixed), and the story `DELETE /:id` route's async transaction callback is incompatible with better-sqlite3's sync-only transaction API (not fixed — unrelated to this feature, flagged as a P2 bug below, ID B7). Brainstorm chats are wired narrower than the other chat types by deliberate design call — see that DECISIONS.md entry for why.

**C1 shipped 2026-07-20, same session** — new `aiChats.includeMemory` toggle + `resolveMemories` in `chatContextService.ts`, surfaced in the same World-Building/Research/Brainstorm chat UI the Notes/Outline toggles landed in. No per-item flag needed on the memory side (every `status: "active"` memory is already index-eligible via Phase B's own approve step). See `DECISIONS.md` "Project Memory Chat Toggle (C1)".

**Canonical design (Notes/Outline ↔ chat):** `docs/Notes_Outline_Chat_Bridges_Design.md` (locked 2026-07-18).

#### Persistence & project packaging (required with bridges — locked)

| # | Recommendation |
|---|----------------|
| 1 | Treat **`notes` / outline item rows as SoT** — RAG is derived cache only; toggle-off never deletes the row |
| 2 | Add **notes (+ `includeInAi`)** to **story export/import** (`GET /stories/:id/export` omits them today) |
| 3 | Add **outline items (+ `includeInAi`)** to story export/import (same packaging gap) |
| 4 | **Do not export RAG chunks** — reindex after import from rows + flags |
| 5 | **Project Saves timelines stay Codex/chapter-focused** unless note/outline history is explicitly reopened later; portable package = story JSON export/import |

Also: extend **`reconcile_index`** valid keys for armed notes/outline only — never a job that deletes note rows as “orphans.”

#### Notes/Outline ↔ chat slices

| Slice | Description |
|-------|-------------|
| N0 | ✅ Story export/import includes notes (+ outline); round-trip `includeInAi`; no RAG blobs in export |
| N1 | ✅ `notes.includeInAi` (default false) + UI toggle/badge (bulk enable not built — single-item toggle only) |
| N2 | ✅ RAG `entityType: "note"` only when armed; remove on off/delete; reconcile_index |
| N3 | ✅ Per-chat `includeNotes` / `includeOutline` (default false) on **all chats except Editor** — including Brainstorm, wired narrower (see DECISIONS.md) |
| N4 | ✅ `chatContextService` non-canon packets when **both** gates pass (top-K, labeled working material) |
| N5 | ✅ Manual "Save message as note" — hover action in `ChatMessageList.tsx` (both chat surfaces), `NoteFormDialog.tsx` reused |
| N6 | ✅ AI `note-proposal` fence + accept/reject `NoteProposalCard.tsx` — non-Editor `features/chat` chats only (not Brainstorm, see DECISIONS.md) |
| O1–O4 | ✅ Same double-gate pattern for outline items |

**Inclusion doctrine:** per-item AI flag AND per-chat toggle, both default OFF; Editor never gets these toggles.

#### Existing memory/scanner slices (any order)

| Slice | Description |
|-------|-------------|
| C1 | ✅ Opt-in **“Include project memory”** for chat / context (`entityTypes` includes `agent_memory`) |
| C2 | ✅ **“Suggest memories from this scan”** button (per completed scan, `RagScannerPanel.tsx` scan history) → enqueues `distill_memory` via the existing generic `POST /api/agent/jobs` (manual only; no silent auto-chain) |
| C3 | ✅ Scanner may **read active** `agent_memory` when opted in per-scan (`includeMemory` toggle in `RagScannerPanel.tsx`/`ChapterScannerDrawer.tsx`, carried as the job's own `payload.includeMemory` — not a persisted setting) — factual contradiction checks via a second scanner system prompt variant |
| C4 | ✅ Per-story **unattended** `rag_scan_story` schedule toggle (default OFF) — `stories.unattendedScanEnabled` column (migration `0035_odd_nextwave.sql`), fixed daily cadence in `jobRunner.ts`'s schedule tick, toggle in `RagScannerPanel.tsx` |
| C5 | ✅ **Codex auto-compile from manuscript** — done 2026-07-20, shaped exactly per this row's own recommendation: manual-trigger-only new `suggest_codex_updates` job (`codexCompileJob.ts`), never auto-chained, proposing updates to **existing** Codex-enabled entries only (wardrobe/appearance/wounds/items — never customFields/description/new entries) through the *existing* `codexPendingChanges` approve/reject pipeline (`sourceType: "ai"`, previously unused). New review surface added since the existing chat-scoped tray couldn't show job-sourced proposals: `CodexPendingChangesPanel.tsx` (entry-scoped, source-agnostic) next to `CodexHistoryPanel.tsx` in the Lorebook entry editor. Trigger button ("Suggest Codex updates from this chapter") lives in `ChapterScannerDrawer.tsx`. New `codex_compile` per-feature endpoint key. `useReviseProposalMutation`'s "Edit First" was NOT extended to this new panel (Approve/Reject only — no chat to revise through); still only wired for the chat-scoped tray, per the R0-R3 entry. See `DECISIONS.md`'s "P0.3 Remaining Slices (N5/N6, C2-C5)" for the full trail, including a documented stale-dev-server artifact (the user's other running session needs a restart to pick up the new job type/route/schema changes). |

**Refs:** `docs/Notes_Outline_Chat_Bridges_Design.md`; `docs/Chat_Panel_Integrations_Design.md`; Agent design Phase C; `DECISIONS.md` Phase B (no auto-distill; no suggest UI yet)

**Clarification 2026-07-19:** Keep agent memory **within existing plan** (Project Memory + Phase C as already designed). Do **not** add new unplanned agent-self / always-on / silent-learning memory. Ship planned C1–C4 and Phase C items; no scope expansion beyond the design docs.

---

### P0.4 — Chat ↔ panel integrations (selection rework + host chats)

**Status:** R0-R3 — ✅ Done (2026-07-20). R4/R5/R7/R8 — ✅ Done (2026-07-20, same day). B0-B4 (Brainstorm Hub) — ✅ Done (2026-07-20, same day). B5 (WB + Outline guided-start, Character psych module) — ✅ Done (2026-07-20, same day). R6 (auto-insert/auto-accept toggles) — ✅ Done (2026-07-20, same day). S0-S5 (Research web desk) — ✅ Done (2026-07-21). K0-K5 (Notes desk) — ✅ Done (2026-07-21). **All of P0.4 is now done.**  
**Canonical design:** `docs/Chat_Panel_Integrations_Design.md`

**Doctrine:** Panel owns artifact; **chat governs** content; selection/focus owns span; Accept applies; no amnesiac one-shot as primary path.

**R0-R3 shipped 2026-07-20** — see `DECISIONS.md` "Editor Selection Rework + Codex Proposal Tray (P0.4 R0-R3)" for the full load-bearing trail, including a real correctness bug fixed in `activeChapterEditorStore.ts` (single global slot → per-chapter map) and a second pre-existing bug found and fixed in `useCreateChatMutation`/`useUpdateChatMutation`'s cache invalidation (was invalidating a key that never matched any real query). Full live Accept round-trip (real LLM reply → replace selection) could not be exercised in this dev environment — the configured AI provider returned zero content tokens for reasons unrelated to this feature's code (see that DECISIONS.md entry) — every other piece (capture, chat binding, context delivery, tray) was verified live.

**R4/R5/R7/R8 shipped 2026-07-20, later the same day** — see `DECISIONS.md` "P0.4 R4/R5/R7/R8 — Lorebook Rework → WB, Dedicated Outline Chat, Outline Row Rework, WB Handoff" for the full load-bearing trail. Note: the backlog line below previously called R6-R7's `chatType` split "deliberately deferred — neither R0-R3 nor R4/R5/R8 strictly require it"; that was wrong for R7 specifically — Outline had no chat identity of its own (it was reusing `EditorChatRail` with `chatType="editor"`), so a real dedicated Outline chat (R5) was impossible without R7's split, which happened here as part of R5. **R6 (auto-insert/auto-accept toggle UI) stays deferred** — genuinely optional, every new proposal path here defaults to manual accept with no new toggle added. Found and fixed one real pre-existing bug along the way: `ReworkCard.tsx`'s trailing hint text was hardcoded chapter-specific ("...in the chapter"), misleading for the new Lorebook/Outline rework cases — now a `hostHint` prop varying per `FocusTarget.kind`. R4 shipped narrower than the design doc's host matrix: description-field sub-span rework only, not per-row structured-Codex-field (wardrobe/wounds/items) rework — cut for cost/value (3 components deep, no `entryId` threaded, low marginal value for short single-line fields), flagged as a fast-follow. Live-verified end to end except the actual model-generated `outline-proposal`/`lore-suggestion` fence content and the resulting "Open in WB" handoff — same "no reachable AI provider in this dev sandbox" limitation every session since R0-R3 has hit.

**B0-B4 shipped 2026-07-20, still the same day** — see `DECISIONS.md` "P0.4 B0-B4 — Brainstorm Hub" for the full load-bearing trail. Migrated Brainstorm off a fully separate parallel chat implementation (own `ChatInterface`/`ChatList`/generation hook, never touching `chatContextService`) onto the shared stack with its own `chatType: "brainstorm"`. Confirmed with user via `AskUserQuestion` before building: B2's "playbook" depth is prompt-driven (Light/Standard/Grill-me system-prompt hints shaping ordinary chat), not a scripted ask/capture/confirm state machine — the only persisted "slot" concept is a fixed 5-entry known/unknown checklist (`brainstormSlots` table). Two new fences (`overview-proposal`, `handoff-packet`) persist immediately on parse as durable `brainstormChecklist` rows — a genuinely different status lifecycle (`pending→opened→done/dismissed`) than `codexPendingChanges`, since B4 requires Open/Send/Accept to perform the real write without clearing the Active queue. All four handoff destinations (Outline/WB/Notes/Research) live-verified end to end via manually-inserted checklist rows standing in for live model output (same AI-provider-unreachable limitation as every prior P0.3/P0.4 session). Found and fixed one real, previously-undiscovered bug along the way, unrelated to this feature's own code but blocking its Notes handoff: `POST /api/notes` never set the `NOT NULL` `updatedAt` column, silently 500ing on every note-creation path in the app (Notes tool's own "New Note" button included) — two-line fix, no caller behavior change.

**B5 shipped 2026-07-20, still the same day** — see `DECISIONS.md` "P0.4 B5 — WB + Outline Guided-Start Playbooks + Character Psych Module" for the full load-bearing trail. User confirmed via `AskUserQuestion` to include Outline in this slice (its own locked spec didn't explicitly require guided-start UI, unlike WB's), for parity across all three playbook-style chats. `GuidedSetupControl.tsx` generalized (moved `features/brainstorm/` → `features/chat/`) so WB and Outline reuse the same shell with their own blurb/opening-lines/style-hint text (`aiChats.wbStyle`/`outlineStyle`, new `WB_STYLE_HINTS`/`OUTLINE_STYLE_HINTS`). WB's Character template gets an opt-in psych module (MBTI/Enneagram/blurb) via a new `psych-proposal` fence — deliberately **not** routed through `codexPendingChanges`/`codexSnapshots` (which stay concrete-state-only per CLAUDE.md's standing constraint); writes straight to `metadata.psychProfile` via the *existing* generic `PUT /api/lorebook/:id` route, no new server route needed. Found and fixed a real correctness bug affecting B0-B4's `brainstormStyle` too, not just B5's new columns: `ChatInterface.tsx`'s context-fetch effect never included any style/psych value in its dependency array, so changing style or the psych toggle updated the DB correctly but the component's own cached system-prompt context silently stayed stale for the next message until something else happened to refire the effect — fixed by adding the four style/psych fields to the dependency array (safe since every caller already passes a fresh chat object on change, not a mutation).

**R6 shipped 2026-07-20, still the same day** — see `DECISIONS.md` "P0.4 R6 — Auto-insert/Auto-accept Toggles" for the full load-bearing trail. Three new `aiChats` columns (`autoInsertProse`, `autoAcceptCodex`, `autoAcceptOutline`, migration `0038_motionless_human_torch.sql`), all default false per doctrine ("no silent canon unless an explicit toggle is ON"). `autoInsertProse` (Editor only) and `autoAcceptOutline` (Outline only, create/edit/reorder — **delete deliberately excluded**, matches the design doc's asymmetric table) live in `ChatInterface.tsx`; `autoAcceptCodex` (Editor/WB/Outline, gated on the existing `usesCodexTray` computation) chains a `useApproveProposalMutation` call right after `useChatMessageGeneration.ts`'s existing `createProposalMutation` succeeds — same call `ProposalTrayCard`'s manual Approve button already makes. Prose auto-insert reuses a new `applyProseProposal` helper extracted from the pre-existing `handleAcceptProse`, so manual Accept and auto-insert never drift; falls back to the normal review card (never silently drops content) if no matching chapter editor is open. Found and fixed one real pre-existing type gap along the way: `chatsApi.proposeNewEntry` (`src/services/api/client.ts`) called `fetchJSON` with no type argument even though the server always returns `{ entry, snapshot, pendingChange }` — typed it to match `proposeModifyEntry`'s existing shape so the new auto-accept chain's `result.pendingChange.id` access is type-safe. `npm run build` (tsc client+server+vite) clean. Live-verified in the Browser pane: each chat type renders exactly its own toggle subset (Editor: auto-insert prose + auto-accept Codex; WB: auto-accept Codex alongside the pre-existing untouched Guided Setup psych-module toggle; Outline: auto-accept Codex + auto-accept outline), a toggle flip PATCHes and survives a full reload, and — since this dev sandbox still has no reachable AI provider (same limitation every P0.4 session has hit) — the actual auto-accept chain was exercised directly via the REST API (create a pending Codex proposal → immediately approve it, mirroring the new `onSuccess` callback exactly) and confirmed the entry updates and the pending row lands `"approved"`. All test artifacts (a scratch chat, a scratch Codex proposal/snapshot) were cleaned up from the demo story afterward.

**S0-S5 shipped 2026-07-21** — see `DECISIONS.md` "P0.4 S0-S5 — Research Chat Web Desk" for the full load-bearing trail. Research was previously always-Global with no research-specific system prompt at all — it silently fell through `chatContextService.ts`'s `buildSystemPrompt` to the World-Building default, meaning it was told `WORLDBUILDING_FRAMING` **and** `CODEX_PROPOSAL_INSTRUCTIONS` (letting the model propose Codex entries from Research, a real pre-existing violation of the design's "Writes: Lorebook/Codex/outline/prose → None"), fixed here as part of S1 with a dedicated `RESEARCH_FRAMING` branch. Search provider: DuckDuckGo HTML scraping (`html.duckduckgo.com/html/`, the same site the Python `ddgs` package targets) — user's explicit choice over a keyed provider (Tavily/Brave/SerpAPI), no new Settings UI or `aiSettings` column needed. New `server/services/webSearchService.ts` (new `cheerio` dependency, pure-JS HTML parsing) exposes `searchWeb`/`fetchPage`, both fail-soft (return `[]`/`null`, never throw) since they run inline in the chat-send path. `ResearchTool.tsx` gained a Story/Global mode toggle (Story mode reuses `OutlineChatRail.tsx`'s exact most-recent-or-create pattern, no new server route). New `aiChats.webSearchEnabled` column (default **true**, unlike every other opt-in toggle — search is the desk's core job per the design doc). Web search is query-driven, not toggle-gated like Notes/Memory: `getChatContext`'s existing `query` param (previously only used for RAG relevance) now also triggers `resolveWebSearch` when explicitly passed (a new `explicitQuery`, deliberately separate from the RAG path's `chat.title`-fallback `effectiveQuery`, so mount-time context fetches never fire a live scrape) — threaded client-side via a new `extraContext` param on `generate()`/`createPromptConfig` rather than React state, avoiding a stale-closure race. Citations render via existing markdown link rendering (`MarkdownRenderer.tsx` gained a `target="_blank"` override, applies app-wide). Found and fixed a second real bug while restructuring `entityTypes`: `hybridSearch()` builds a SQL `entityType IN (...)` clause that becomes invalid (`IN ()`) with an empty array — Research with every toggle off produces exactly that; guarded in `getChatContext` rather than `hybridSearch` itself. `npm run build` clean. Live-verified in the Browser pane, including real external network calls this sandbox *can* reach (unlike the AI-provider gap every other P0.4 session hits): a live DDG search for "MI6 history" returned 5 real, correctly-unwrapped results; a query containing a Wikipedia URL correctly fetched and truncated that page; the empty-toggle SQL guard confirmed with no error; Story vs Global mode both verified to render the correct toggle subset and independent chat identities; `GET .../context` confirmed `RESEARCH_FRAMING` present and `codex-proposal` absent; "Save as note" confirmed appearing in Story mode and absent in Global mode; citation links confirmed `target="_blank"`; Copy button confirmed present and firing with no console errors. All test chats/messages cleaned up from the demo story afterward.

**K0-K5 shipped 2026-07-21, same day** — see `DECISIONS.md` "P0.4 K0-K5 — Notes Desk" for the full load-bearing trail. Exploration found K5 (wire N1-N6 for other chats) was already fully done (N5 is a generic negative gate, N6 already spliced into every other chat type's framing) and K0's export half (N0) was already done — the real remaining scope was badges/filters/pin (K0 remainder), a whole new `notes` chatType + desk context pack (K1), whole-note rework + split-dump proposals (K2), a promote tray (K3), and dump import (K4). New `notes.pinned` column; `NoteList.tsx`/`NoteListItem.tsx` gained type/armed/search filters, counts, and a pin toggle. New `NotesChatRail.tsx` (mirrors `OutlineChatRail.tsx`'s find-or-create-most-recent pattern, own chat list per the design doc's "own chat list") + new `NOTES_FRAMING` system prompt + two new always-on desk reads (`resolveAllStoryNotes` — every story note's title/type, deliberately not gated by `includeInAi`, a "desk privilege"; `resolveFocusedNote` — full body of whichever note is open in the Notes tool, threaded via a new `focusedNoteId` param on `getChatContext`/`ChatInterface`). Whole-note rework (not sub-span — `react-simple-wysiwyg`'s contentEditable has neither Lexical's node-key selection nor a plain textarea's character offsets, so precise span capture was deliberately scoped out, same kind of narrowing R4 did for structured Codex fields) reuses the existing `note-proposal` fence unchanged, just branching Accept to update instead of create when a `note-item` rework is active. Split-dump reuses a new `note-split-proposal` fence (one fence, array of notes, mirrors `overview-proposal`/`handoff-packet`'s shape) persisted immediately as a `brainstormChecklist` row with a new `note_split` kind — confirmed via exploration that table/service/route already have zero `chatType` enforcement, so this needed only a one-line kind-allowlist bump, no schema change. Promote (→ synopsis / → WB / → Outline) reuses the `overview-proposal`/`handoff-packet` wire format **completely unchanged** — both are already parsed+persisted for any chat type, gated only by whether a chat's own system prompt ever instructs the model to emit them. New `NotesChecklistTray.tsx` (forked from `BrainstormChecklistTray.tsx`, not generalized in place) renders three sections. Import dump is the concrete UI entry point for the split capability, not a separate mechanism — a dialog seeds the Notes chat composer via the existing `pendingChatComposerSeed` pattern. Bonus consistency fix: Brainstorm's own "Handoff → Notes" destination previously created a note directly with no navigation (no chat rail existed); now navigates+seeds the Notes rail like every other destination. **A real, previously-uncaught bug was found and fixed as a prerequisite**: Research's system prompt template (`systemPrompts.ts`'s `"research-system"` seed) never contained the `{{codex_context}}` token, so `RESEARCH_FRAMING`/live web search results built in the S1 session were computed and silently discarded — never actually reaching the LLM. Fixed the seed file (fresh installs) plus a new idempotent boot-time `patchStaleSystemPrompts()` function (already-provisioned installs, including this dev sandbox — confirmed live via the boot log: `"Patched stale system prompt content for promptType 'research'"`). `npm run build` clean throughout. Live-verified extensively in the Browser pane: `NOTES_FRAMING` confirmed present with no Codex/outline/prose instructions; `allNotes`/`focusedNote` confirmed populating correctly as the selected note changed; a manually-posted `note_split` checklist item's "Accept all" confirmed creating both notes with correct title/content/type; a manually-posted synopsis promote's "Accept" confirmed updating the story; a manually-posted WB handoff's "Open" confirmed transitioning the item to `"opened"` status and navigating to the Lorebook tool with the seed set (the same pre-existing, unmodified `pendingLorebookSeed`/`CreateEntryDialog` mechanism Brainstorm's B0-B4 already proved live); the "Rework in chat" button confirmed capturing the note and opening a bound Notes chat with the correct rework framing text. One real bug found and fixed during this same pass: `NotesTool.tsx` initially omitted the `LorebookProvider` wrapper every other chat-hosting tool page has (`ChatInterface` calls `useLorebookContext()` unconditionally) — caught immediately via the Browser pane's error boundary, fixed before the rest of verification continued. All test chats/notes/checklist items cleaned up from the demo story afterward, synopsis restored to its original value.

| Slice | Description |
|-------|-------------|
| R0 | ✅ Shared FocusTarget / FocusPacket / ReworkCard shell (`src/types/rework.ts`, `src/features/rework/`) |
| R1 | ✅ **Editor:** highlight → Rework card → Editor chat (before/after/selection + full editor context) → Accept **replaces selection** |
| R2 | ✅ Buried primary floating one-shot Expand/Rewrite/Shorten behind "More"; now chips that send instructions into host chat |
| R3 | ✅ Editor Codex **tray under chat list** (this chat only, `CodexProposalTray.tsx`); Edit-before-approve wired (`useReviseProposalMutation`, previously unused); auto-accept still default OFF (unchanged — no auto-accept toggle added this pass) |
| R4 | ✅ **Lorebook:** description-field sub-span rework → **WB chat** → `codex-proposal` (`modify_entry`) reviewed via the same tray Editor uses (WB's inline `ProposalCard` retired in favor of it). Structured Codex-field (wardrobe/wounds/items) rework **not included** this pass |
| R5 | ✅ **Outline chat:** own `chatType`/`promptType: "outline"`, own rail/list (`OutlineChatRail.tsx`); always-on full outline tree + written-chapter-summaries context (`chatContextService.ts`); `outline-proposal` fence (create auto-persists as a pending tree row, edit/reorder/delete are ephemeral cards) + auto-accept OFF; **bulk Generate button retired** (`outlineGenerator.ts` deleted); Codex tray reused; note-proposal reused; `lore-suggestion` fence + tray section |
| R6 | ✅ Auto-insert (prose, Editor) / auto-accept (Codex — Editor/WB/Outline; outline create/edit/reorder — Outline only, never delete) toggles per host, defaults OFF. New `aiChats.autoInsertProse`/`autoAcceptCodex`/`autoAcceptOutline` columns; Codex auto-accept chains `useApproveProposalMutation` right after proposal creation in `useChatMessageGeneration.ts`; prose auto-insert/manual Accept share a new `applyProseProposal` helper |
| R7 | ✅ Split `chatType` **editor** vs **outline** (separate chat lists) — done as part of R5, see note above |
| R8 | ✅ Outline row "Rework in chat" (whole title+summary, not sub-span) + tray **Open in WB** from lore-suggestion cards, via the same same-tab `StoryContext.pendingLorebookSeed` pattern the Relationships graph's "Open entry" already used |
| B0–B4 | ✅ **Brainstorm hub:** migrated to shared `aiChats`/`chatContextService`/`ChatInterface` stack (`chatType: "brainstorm"`); CTA+blurb+Guided setup+style dropdown (`GuidedSetupControl.tsx`); depth is prompt-driven (`STYLE_HINTS`), not a state machine, per fixed 5-slot known/unknown checklist (`brainstormSlots`); depth-adaptive `overview-proposal` (synopsis/note/opt-in memory) + `handoff-packet` (Outline/WB/Notes/Research, multi-per-reply) fences; durable tray checklist (`brainstormChecklist` table, `BrainstormChecklistTray.tsx`) — Open/Send/Accept perform the real write but only **Mark done** clears Active. WB handoff reuses `pendingLorebookSeed`; Outline/Research reuse new `StoryContext.pendingChatComposerSeed` + `ChatInterface`'s new `initialComposerText` prop; Notes handoff creates directly. Found/fixed a real pre-existing `POST /api/notes` bug (missing `updatedAt`) along the way. See `DECISIONS.md` "P0.4 B0-B4" |
| B5 | ✅ WB + Outline guided-start (shared `GuidedSetupControl.tsx`, per-host style hints, `wbStyle`/`outlineStyle` columns); Character template **psych module** (MBTI/Enneagram/blurb via new `psych-proposal` fence → `metadata.psychProfile`, Grill-me nudges the toggle on; deliberately outside `codexPendingChanges` — writing aid only, never Codex state). Location playbooks (per `Locations_And_Maps_Design.md`) **not included this pass** — still P3/unstarted, see that doc's own row below. See `DECISIONS.md` "P0.4 B5" |
| S0–S5 | ✅ **Research:** Story/Global mode toggle (`ResearchTool.tsx`, no chat list — two single-chat identities); live web search+fetch via DuckDuckGo scraping, no API key (`webSearchService.ts`, query-driven via a new `explicitQuery`/`extraContext` path, not toggle-gated); citations via existing markdown link rendering (+ new `target="_blank"` override); save-as-note starts working automatically once Story mode gives it a real `storyId`; opt-in Lorebook toggle (reuses Brainstorm's `includeLorebook` column) + Notes; new `enableCopy` Copy button. Fixed a real pre-existing bug: Research previously leaked `CODEX_PROPOSAL_INSTRUCTIONS` via a `WORLDBUILDING_FRAMING` fallthrough — now a dedicated `RESEARCH_FRAMING`. See `DECISIONS.md` "P0.4 S0-S5" |
| K0–K5 | ✅ **Notes desk:** badges/filters/pin (`notes.pinned`, type/armed/search filters + counts on `NoteList.tsx`); own `notes` chatType + chat list (`NotesChatRail.tsx`, mirrors `OutlineChatRail.tsx`) with a `NOTES_FRAMING` system prompt and always-on desk reads (`allNotes`/`focusedNote`); whole-note rework (reuses `note-proposal`, Accept branches update-vs-create) + split-dump (new `note-split-proposal` fence, reuses `brainstormChecklist` with a new `note_split` kind — zero schema change); promote tray (`NotesChecklistTray.tsx`) reusing `overview-proposal`/`handoff-packet` unchanged for → synopsis/WB/Outline; Import dump dialog seeds the Notes chat composer. K5 (N1-N6 double-gate for other chats) confirmed already done pre-K-track. See `DECISIONS.md` "P0.4 K0-K5" |

**Also locked (see design doc):** full §1–§7 (WB, Editor, rework, Outline, Brainstorm, Research, Notes desk).

---

## P1 — Polish on shipped systems

### P1.1 — Agent Framework Phase C (à la carte)

- ✅ **Richer Project Memory UX — done (2026-07-21).** Superseded tab (read-only: Delete only, no Pin/Edit) added to `ProjectMemoryPanel.tsx`. Pin semantics is now a real behavior change, not decoration: `chatContextService.ts`'s `resolveMemories` always includes a story's active pinned memories in chat context (unbounded by `RELEVANT_ENTRIES_LIMIT`), tagged `role: "pinned"` and called out in the assembled prompt text; pinned rows also sort first in the list UI. See `DECISIONS.md`'s "Agent Framework Phase C — Superseded Tab, Pin Semantics, Writer-Pref Browser (P1.1)" entry.
- ✅ **Cross-project `writer_pref` browser — done (2026-07-21).** Global (`storyId: null`) memories previously had no creation path anywhere in the UI. New shared `NewMemoryNoteDialog.tsx` (extracted from `ProjectMemoryPanel.tsx`, `storyId: string | null`) + a new `global` list filter (`agentMemoriesRepository.ts`/route/client) + a new `WriterPrefsCard.tsx` on the Settings page (alongside `RecentJobsCard`), defaulting new notes to category `writer_pref`. Same DECISIONS.md entry.
- Soft concurrency for non-overlapping jobs (only if serial becomes painful) — **not done, no signal it's needed yet**
- Migrate fully off `ragScans` dual-write (only when UI/API consumers are ready) — **not done, consumers not yet migrated**

### P1.2 — Relationship Graph G1.5+ — ✅ Fully done (2026-07-22)

- ✅ **Pending-edge review — done (2026-07-21).** `GET /stories/:storyId/graph/pending` + `POST /graph/edges/:id/approve`/`reject` (mirrors `agentMemories.ts`'s approve/reject idiom), new `PendingEdgesPanel.tsx` surfaced as a third "Pending" tab (with live count badge) on `StoryGraphCanvas.tsx`, alongside Ego/Full. Since nothing previously produced non-`active` rows, a minimal manual producer was added too: an opt-in "Propose for review" `Switch` on `EdgeEditDialog.tsx`'s create form (`asPending` → `status: "pending", source: "user"`, no new `source` value). `approveEdge` re-checks the active-edge conflict at approval time (not just creation time) and returns a clean 400 on conflict rather than a raw constraint error. No schema/migration change needed — `status` already existed for exactly this. Deliberately **not** built this pass: AI-suggested edges themselves (separate item below), dashed-edge-in-canvas rendering / click-to-approve on the graph itself (review is a dedicated list tab instead), persisted layout. See `DECISIONS.md`'s "Lorebook Relationship Graph — Pending-Edge Review UI (P1.2)" entry.
- ✅ **AI suggest edges — done (2026-07-22).** New `graph_suggest_edges` job (manual-trigger only, same precedent as `distill_memory`/`suggest_codex_updates` — never auto-enqueued), story-wide (not chapter-scoped, capped at 60 entries alphabetically), writes `status: "pending", source: "ai_suggested"` via a new `storyGraphService.proposeAiSuggestedEdge` that silently skips rather than throws on an already-active conflict. Owner-only "Suggest relationships" button in `StoryGraphCanvas.tsx`'s toolbar. Output lands in the existing Pending tab completely unchanged — approve/reject never discriminated by `source`.
- ✅ **Persist layout — done (2026-07-22).** New `storyGraphLayout` table (`storyId`, `nodeId`, `x`, `y`, unique per story+node). Full-graph view only (ego view's BFS-ring layout is relative to whichever entry is centered, not a stable absolute position, so it's excluded by design) — merges saved positions over `layoutGrid`'s computed fallback, persists on `onNodeDragStop`, "Reset layout" button clears it.
- ✅ **Reindex lorebook text on edge change — done (2026-07-22).** `buildLorebookEntryText` (`ragIndexService.ts`) became async and now appends a `"Relationships:"` block from each entry's active edges; `reconcileIndexJob.ts`'s staleness check updated to match (avoiding a scenario where reconcile would "fix" the richer text back to entry-only every 15 min). `storyGraphService.ts` fires a fire-and-forget reindex of both endpoints on every active-state transition (create/update/approve/delete of an active edge, plus migration) — pending/rejected edges never reindex, since they were never in the indexed text.

See `DECISIONS.md`'s "Lorebook Relationship Graph — G1.5+ (AI-Suggested Edges, Persisted Layout, RAG Reindex-on-Edge-Change)" entry for the full load-bearing trail, including why the reindex change had to make `buildLorebookEntryText` async rather than just bolting relationship text onto `indexLorebookEntry` alone.

**Refs:** `docs/Thin_Story_Graph_And_Lorebook_Visualization.md`; `DECISIONS.md` Lorebook Relationship Graph

### P1.3 — Chat context — ✅ Done (2026-07-23)

Per-message RAG re-seeding, closing the gap explicitly deferred in the "Chat Context Anchoring" DECISIONS.md entry. **World-Building/Editor/Outline chats only** (user-confirmed scope) — Research already has its own query-driven search, Brainstorm uses manual context selection, Notes has its own always-on desk reads. On each send, `ChatInterface.tsx`'s `doSend` now fetches `chatsApi.getContext(selectedChat.id, input)` (current outgoing message only as the query — same precedent as Research's existing `explicitQuery`), filters the response to `role === "search"` entries (anchor/related content is already unconditional in the mount-time `codexContext` block, so re-including it would just duplicate it), and threads the result through as `extraContext` — merging with, not replacing, the mount-time title-seeded fetch, via the same `[codexContext, reworkContext, extraContext]` join Research's `extraContext` already established. Fires on every send, no throttle (`hybridSearch` is a local synchronous SQLite query, not a network scrape). Zero server-side changes needed — `GET /:chatId/context?query=...` already supported an arbitrary query and already merged anchor-first/search-tail. Live-verified in the Browser pane against the real dev DB (a session with a reachable AI provider, for once): sent an off-topic message in each of a WB, Editor, and Outline chat and confirmed via network inspection that the new `?query=...` request's `role: "search"` results correctly re-ranked to match the message's actual topic (e.g. asking about "Marcus Webb's assignment" surfaced Marcus Webb top-of-list instead of the stale title-seeded set), and that the real generated replies (Editor/Outline) reflected the fresh context. All test messages cleaned up from the demo chats afterward via direct API calls. See `DECISIONS.md`'s "P1.3 — Per-Message RAG Re-Seeding" entry.

### P1.4 — Visible AI Reasoning — ✅ Done at user-confirmed scope (2026-07-23)

The locked design doc (`docs/2026-06-26_Visible_AI_Reasoning_Design.md`, added to the repo 2026-07-23) scoped this to **both** chat windows **and** scene/generation output ("Applied to regular chat and to generation features (scene writing, etc.)"). **User explicitly narrowed this to chat windows only** — scene/generation output display is declined, not deferred.

At chat-only scope, this was **already fully built**, nothing left to implement:
- `<think>`/`<thinking>`/`<reasoning>` tags are parsed (`src/utils/parseThinking.ts`) and rendered only inside `ChatMessageList`/`AssistantMessageContent.tsx` — the shared message-rendering path every chat type imports through `ChatInterface.tsx` (WB, Editor, Outline, Research, Notes, Brainstorm). No other AI output surface (chapter prose, prose-proposal preview, RAG Scanner, Name Generator, image generation, Humanizer, TTS, or any Agent Framework background job) touches it, matching the requested scope exactly.
- A global on/off toggle already exists — "Show Model Thinking" switch in the Dashboard's Preferences sidebar (`PreferencesSidebar.tsx`), backed by a cross-component localStorage flag (`src/lib/useShowModelThinking.ts`, defaults **off**). When off, thinking blocks don't render at all (not even collapsed); when on, each message with thinking content shows a collapsible "Model thinking" section (`ThinkingBlock.tsx`, collapsed by default, click to expand).

**Explicitly declined, not just out of scope for this pass:** scene/generation-output display (the design doc's other half) and everything the doc itself already excluded (agentic reasoning traces, Codex/memory/state deltas, RAG Scanner integration, approval workflows, trace history/versioning, multi-step agent visibility).

**Closed permanently, user-confirmed 2026-07-28** — with the chat-window half fully built and shipped, the design doc's other half (scene/generation-output display) does not remain open as a future candidate. Don't re-offer it as a "could reopen" item; treat P1.4 as fully resolved at its shipped scope.

**Refs:** `docs/2026-06-26_Visible_AI_Reasoning_Design.md` (v1.1, "Narrow Scope"); `DECISIONS.md`'s "P1.4 — Visible AI Reasoning" entry

### P1.5 — Per-message action bar (Copy/Delete/Regenerate/Edit) — ✅ Done (2026-07-27)

User request (LM Studio parity): each message bubble — user and assistant — should have hover controls, not just the current scattered/partial set. Since **every chat type (WB, Editor, Outline, Research, Notes, Brainstorm) already renders through the one shared `ChatInterface.tsx` + `ChatMessageList.tsx`**, building this once in that shared pair covers all chat types for free — no per-desk duplication needed. Messages are stored as a single JSON array per chat row (`aiChats.messages`, no per-message table), so server support is "load row → mutate array → `updateChatMessages`," not a schema change.

**Branch (forking a conversation) explicitly excluded from this plan** — confirmed out of scope for now (biggest lift, no existing lineage concept; can be requested separately later).

Current state (audit, 2026-07-25): Copy exists but only on assistant messages and only when a chat opts in via `enableCopy` (Research today). Save-as-note exists on both roles. Edit UI exists in `ChatMessageList.tsx` (assistant-only, textarea+Save/Cancel) but `ChatInterface.tsx` never actually passes `onStartEdit` — so today, **edit is not wired up anywhere**, despite the affordance existing in the component. Delete and Regenerate don't exist at all.

**Staged plan:**

| Stage | Scope |
|-------|-------|
| MB0 | ✅ **Done (2026-07-25).** Copy, both roles, unconditional — dropped the `enableCopy` gate (was Research-only), Copy now renders on every assistant *and* user message hover toolbar in `ChatMessageList.tsx`. Also added a Copy button to the chat-bubble text-selection bar (same file) alongside Save-as-note/Send-to-Notes/Dismiss. Client-only, no server change. |
| MB1 | ✅ **Done (2026-07-27), scope-narrowed.** No new server routes needed — the generic `PATCH /:chatId {messages}` full-array-replace (`chatsApi.update`) already existed and already round-trips through `replaceMessages`/`updateChatMessages`. MB2-MB4 all build on that one existing endpoint via client-side array mutation instead of new single-message routes. |
| MB2 | ✅ **Done (2026-07-27).** Delete, both roles — trash icon in `ChatMessageList.tsx`'s hover toolbar, `ConfirmDialog` gate in `ChatInterface.tsx` (`pendingDeleteMessage` state), filters the message out and PATCHes the full array back. |
| MB3 | ✅ **Done (2026-07-27).** Edit, both roles — the existing (previously assistant-only, previously unwired — `onStartEdit` was never passed) textarea edit UI now applies to user messages too. Locked the open question as recommended: assistant edits rewrite content in place (`edited`/`editedAt`/`editedBy`/`originalContent` fields, no cascade); user edits do the same field update but also truncate everything after that message (`selectedChat.messages.slice(0, idx)` + the edited message), since a later reply was responding to text that no longer exists once the user's own turn changes. |
| MB4 | ✅ **Done (2026-07-27).** Regenerate, assistant messages only — new button walks backward from the target reply to the nearest preceding user message, truncates from there, persists, then re-invokes `generate()` with that user message's own text. Deliberately re-runs a full fresh send (including a fresh `computeExtraContext` RAG/web-search fetch, extracted from `doSend`) rather than replaying stale context from the original send. |

Live-verified in the Browser pane against the real dev DB: MB2/MB3/MB4 confirmed end to end on a disposable scratch chat (created and deleted via direct API calls) — delete removes exactly one message with no cascade, assistant edit rewrites in place, user edit truncates correctly, and Regenerate produced a real fresh assistant reply from a reachable cloud provider this session. **Incident during testing:** MB3's edit-truncate behavior was first verified against a live, non-disposable World-Building chat (`Character Codex`, Nana Usmanov, Jul 25) instead of scratch data — editing its first (user) message correctly truncated ~20 messages of real prior conversation per the designed behavior, which is real data loss, not a bug. Chat isn't part of the static demo-seed fixture and there's no automated DB backup in this project (`docs/DEPLOYMENT.md` only documents a manual `cp`). User confirmed (2026-07-27) to leave it and move on rather than attempt DB-level recovery. **Lesson, same one Outline Import's own verification note already flags:** use a disposable scratch chat/story for any testing that can mutate or truncate real conversation history, not existing demo data.

MB0-MB4 all shipped, closing out P1.5 in full — no stage was left half-done.

---

## P2 — Known bugs / debt

| ID | Issue | Notes |
|----|--------|--------|
| B1 | **Word count reads 0** | ✅ **Fixed 2026-07-21.** `WordCountPlugin`: stable `useMemo`+`debounce` (was recreating debounced fn every render via `useCallback(() => debounce)()`); public `$getRoot().getTextContent()` instead of private `_editorState._nodeMap`; initial count on mount. Still reports toolbar + `chapterWordCountStore` for focus goals. |
| B2 | **Beat mark text deleted in editor leaves DB row** | ✅ **Done (2026-07-22).** `BeatMarkSyncPlugin.tsx` now registers a `registerMutationListener(BeatMarkNode, ...)`: on a "destroyed" mutation, it reads the mark's beat id(s) from `prevEditorState`, then checks the post-mutation document for any surviving `BeatMarkNode` with that id (guards against split/rebuild edits that destroy+recreate within the same transaction) — only ids truly gone from the whole document call the existing `useDeleteBeatMutation`, the same cleanup path the panel's trash button already used. Live-verified in the Browser pane: marking a word as a beat then deleting it in-editor (not via the panel) now auto-fires "Beat removed" and the row drops out of the Concrete Beats panel; editing adjacent/unrelated text leaves an untouched beat mark alone; undo of the mark-creation itself correctly cleans up the row too (same orphan risk via a different path, closed for free). |
| B3 | **Global/series lorebook not in RAG** | By design for now; multi-story association needed later |
| B4 | **Story scan job restarts from chapter 0 after crash** | ✅ **Done (2026-07-22).** `runRagScanStoryJob` (`ragScanJobs.ts`) now resumes instead of restarting: `agentJobs.progress` gains an optional `scanId` field, written *before* the chapter loop starts (not just after each chapter, so even a crash on the very first chapter of an attempt still leaves it pointing at the right row) alongside the existing `processed`/`total`. On (re)entry, if `progress.scanId` resolves to a `ragScans` row still `status: "running"`, that row is reused and `listOrderedChapterIds` is sliced from `progress.processed` instead of 0; otherwise it falls back to a fresh scan exactly as before (first attempt, or a corrupt/stale scanId can't wedge the job). No migration needed — `progress` is an untyped JSON column. Verified with a disposable integration script (`_b4_check.ts`, deleted after the run): seeded a scratch story + 3 chapters, fabricated a `ragScans` row at 1/3 processed with an `agentJobs` row pointing at it, called the real job handler directly, and confirmed it reused the same scan id, finished at processedChapters=3/status=completed, and `agentJobs.progress` still carried the same `scanId` — no duplicate scan row was created. |
| B5 | **Legacy `metadata.relationships` wiped on lorebook save** | Edge table is SoT; don’t re-depend on metadata JSON for links |
| B6 | **Stale design-doc headers** | ✅ **Done (2026-07-22).** Updated top `**Status:**` lines in `Agent_Framework_And_Project_Memory_Design.md`, `Thin_Story_Graph_And_Lorebook_Visualization.md`, `Chat_Panel_Integrations_Design.md`, and `Notes_Outline_Chat_Bridges_Design.md` — all four still said "not implemented" despite their features (Agent Phase A/B, Graph G1/G1.5+, P0.4 R0-R8/B0-B5/S0-S5/K0-K5, P0.3 N0-N6/O1-O4/C1-C5) being long shipped. `Chat_Model_Routing_And_Chrome_Design.md`'s one "not implemented" mention was checked and left alone — it's describing the pre-fix problem statement, not a live status claim; its own Status line was already accurate. `Locations_And_Maps_Design.md`'s "not implemented" is also accurate (genuinely unstarted P3). |
| B7 | **Story `DELETE /:id` always 500s** | ✅ **Done (2026-07-21).** `db.transaction(async tx => {...})` — better-sqlite3's transaction API is sync-only and rejected the async callback with `TypeError: Transaction function cannot return a promise`. Fixed in `server/routes/stories.ts`'s delete route: callback is now a plain sync function, each query forced to execute via `.run()` instead of `await`. Live-verified against the dev server: scratch story with a story-level lorebook entry, org folder, and chapter → `DELETE` returned 204 with no server errors, refetch 404s, lorebook entry and folder both confirmed cascade-deleted. `npm run build` clean. |
| B8 | **Lorebook browse density** | ✅ **Done (2026-07-21).** Doc: `docs/Lorebook_Browse_Density_Design.md`. Slices **L0–L3** all shipped: global `sn-lorebook-browse-view` localStorage pref + segmented Cards\|List control (`LorebookEntryList.tsx` toolbar); compact `LorebookEntryRow.tsx` (thumb · name · level badge · importance · up to 3 tag chips, no description/category chip) vs. extracted `LorebookEntryCard.tsx` for the existing card grid; smart default (List when selected category's entry count ≥ 12, else Cards) via `useLorebookBrowseView.ts`, recomputed each render only while no explicit pref is stored; row actions hidden-until-hover on desktop (`sm:` breakpoint), always-visible on touch. Live-verified in the Browser pane: toggle, smart default at low count, list-row content, reload persistence, click-to-open still routes into the entry tab. |
| B11 | **Editor MultiView: closing/switching a pane could wipe a different chapter's content** | ✅ **Done (2026-07-29).** Real data loss found live: a docked pane's/floating panel's chapter editor tree had no `key={chapterId}`, so React reused the same `LexicalComposer` instance (including `SaveChapterContentPlugin`) across a chapter swap whenever the target chapter was already warm in the React Query cache — `EmbeddedPlayground`'s own "not yet loaded" placeholder normally masks this by accident, but not once data is cached. In the gap between the reused save-listener re-arming for the new chapterId and `LoadChapterContentPlugin`'s microtask-deferred content swap, any update event persisted near-blank content under the new (real) chapterId. Fixed with `key={content.chapterId}` on `PaneTabContentView.tsx`'s `EditorPaneProvider` (forces a real remount on every chapter change — covers both docked panes and floating panels, which share this one render path) plus defense-in-depth: `LoadChapterContentPlugin` now tags its `setEditorState` calls `"chapter-load"`, and `SaveChapterContentPlugin` refuses to persist anything until it's observed that tag for its current chapterId, closing the underlying mount-race even if a similar remount-hygiene bug recurs elsewhere. See `DECISIONS.md`'s "Editor MultiView — Cross-Chapter Content-Loss Bug" entry. |
| B10 | **Guide content is stale vs. shipped features** | ✅ **Done (2026-07-24).** Four new top-level Guide topics added to `GuideTabs.tsx`'s existing registry pattern (one static import + `TabsTrigger`/`TabsContent` per topic, shared by both the standalone `/guide` route and Settings' "Guide" sub-nav tab, no other wiring needed): **Settings & Navigation** (`settings-nav.mdx`, after Basics — sidebar reshuffle, Settings' actual 10-tab sub-nav order, and the "New tab" multi-select opener with an explicit `[!IMPORTANT]` callout on the Chrome/Edge popup-blocker caveat and the real toast-warning wording); **Locations & Maps** (`locations-maps.mdx`, after Lorebook Guide — location Guided setup, the place sheet fields, Story Map's edge-type vocabulary, and "Track Place State" place-Codex versioning); **Name Generator** (`name-generator.mdx`, after Concrete Beats); **Chat Features** (`chat-features.mdx`, after Prompt Guide — Cloud|Local model routing, the collapsed "Context & memory" disclosure, the Context/Token meter chip, and Chat Shuttle). Plus a small addition to the existing Advanced Guide's Content Management section (`### Formatting Text`, noting the editor toolbar's new list support) since no topic mentioned rich-text formatting at all before. UI copy (button labels, field names, toast text) was pulled from the actual component source, not paraphrased, for accuracy. Deliberately not covered (out of scope, not named in this item): Research/Notes/Brainstorm desks, Relationship Graph, Project Memory, Agent Framework — none of those chat/tool surfaces have Guide topics today either, pre-existing gap, flagged as a possible fast-follow rather than folded into this pass. `npm run build` clean; live-verified in the Browser pane — all 4 new tabs plus the `advanced.mdx` addition render correctly (headings, lede, alert callouts, tables) alongside the unchanged existing 10 topics. |
| B9 | **Folders (cosmetic org)** | ✅ **Done (2026-07-21).** Doc: `docs/Folders_Org_Design.md`. Slices **F0–F5** all shipped: new `orgFolders` table (one shared engine — `kind: lorebook\|chat`; lorebook scopes by `level`+`scopeId`+`category` matching `lorebookEntries`' own convention, chat by `scopeId`=`storyId`+`chatType`) + `folderId` on `lorebookEntries`/`aiChats`; `server/services/folderService.ts` (depth ≤3 validation, delete-with-reparent — never cascades, scope/kind match validators); `server/routes/folders.ts`; `LorebookFolderSidebar.tsx` (tree sidebar, folder+descendants filtering with an "Include subfolders" toggle, path-crumb on cards/rows) and `ChatList.tsx`'s folder-tree upgrade (shared across every desk using it — Editor/Outline/WB/Brainstorm/Notes — via one component); shared `src/features/folders/` module (hooks, `folderTree.ts`, context menu/move-to/name dialogs, DnD leaf-filing). One deviation from the original design: folder-onto-folder reparenting is "Move to…" only, not DnD (`OutlineTree.tsx`'s own precedent against unifying cross-level DnD) — leaf-onto-folder DnD ships as designed. Story/series export-import round-trip folders (two-pass id remap, same technique as `outlineItems.parentId`); story/series delete cleanup added. Live-verified in the Browser pane: folder CRUD, delete-reparent, drag-file (verified functionally — headless drag simulation doesn't reliably trigger dnd-kit's pointer sensor), export/import round-trip, B8's Cards\|List/search/sort regression-free. See `docs/Folders_Org_Design.md`'s document history for the full trail. |

---

## P3 — Nice-to-haves / old roadmap (not current foundation)

| Item | Notes |
|------|--------|
| **Planning talk list (queued topics)** | **Living list:** `docs/SN_Planning_Talk_List.md` (**hygiene 2026-08-09** vs this backlog). **T1–T4 closed/shipped.** **T5** Lore Sheet + Sync — ✅ **FS0-FS8 all shipped (2026-08-09)** (`docs/Lore_Sheet_And_Sync_Design.md`). **T6 Story Timeline** — ✅ **TL0-TL12 all shipped.** **Maps v2** — ✅ **fully shipped.** **T7 Notes desk org + browse** — ✅ **NO0-NO6 all shipped (2026-08-08)**. **T8** UX/UI polish cleanup (full toolbox, not feature cull) — **parked**. **Relations · note pins** parked (not T7). **PP6** playbook starter prose still open. **Dep pack 3** ROI freeze 2026-07-27. |
| **Notes desk org + browse UI (T7)** | ✅ **NO0-NO6 all shipped (2026-08-08).** Doc: `docs/Notes_Org_Browse_Design.md`. Lorebook-shaped Notes desk on top of shipped K0–K5: sticky **Browse** tab + closable **note tabs** (`NotesTabStrip.tsx`, persisted per story, mirrors `LorebookTabStrip.tsx`'s pattern exactly); **Cards\|List** toggle (`useNotesBrowseView.ts`, mirrors `useLorebookBrowseView.ts`, same ≥12 smart-default threshold) on the new `NotesBrowsePane.tsx`, replacing `NoteList.tsx`/`NoteListItem.tsx` (retired) with `NoteCard.tsx`/`NoteRow.tsx`; B9 `orgFolders` engine extended with a third `kind: "notes"` (`notes.folderId` column, `resolveNotesFolderId`, `NotesFolderSidebar.tsx`/`NotesFolderTreeRow.tsx` — own presentational tree over the shared `useFoldersQuery`/`folderTree.ts` data layer, same posture Chat's own folder tree already established) — depth ≤3, delete-reparents-never-cascades, drag-to-file via `DraggableLeaf`/`FolderDropZone`, export/import round-trip; hybrid org (folders + type/pin/armed filters + smart piles **All\|Unfiled\|Pinned\|Armed\|Recent**, Recent = 14-day window); title+body search with desk-wide default scope + optional **This folder only** + path crumbs; thin optional tags (`notes.tags` nullable JSON column, `NoteTagsInput.tsx` chip input, filter/search match, never touches RAG); dismissible starter folder seeds (Research/Continuity/Scene scraps/Promotions/Archive) on first empty visit. AI gates (`includeInAi`/`includeNotes` double-gate, desk-privilege reads, note-proposal/split/promote) confirmed unchanged — folders/tags are filing-only, never reach RAG or chat context. Title/type editing (previously a Browse-hover "Edit" dialog) moved into the note's own tab as inline fields, since the locked design's hover-action list didn't include Edit — avoids both a silent capability regression and a design deviation. New Guide topic (`notes.mdx`, registered in `GuideTabs.tsx` after Chat Features). See `DECISIONS.md`'s "Notes Desk Org + Browse UI — T7 (NO0-NO6)" entry for the full load-bearing trail. |
| **UI visual direction** | **Locked 2026-07-21, chrome pass complete 2026-07-21** (talk list **T1**). Chrome: **Linear A + Raycast accents**. Doc: `docs/UI_Visual_Direction.md`. Canonical mocks: Eclipse + Light Linear+Raycast PNGs in `docs/design-mocks/`. Token/chrome pass only; no shell rewrite. **V0/V1/V2 all done 2026-07-21** — V0: per-theme `--raycast-a`/`--raycast-b`/`--accent-glow`/`--accent-glow-strength` + tightened `--radius`. V1: topbar gradient hairline, sidebar active-rail bar+glow, cmd-palette hover glow, opt-in `Button variant="gradient"` (demoed on Notes' empty-state CTA). V2: chat user-bubble ring+glow, Research Story/Global pill gradient underline — deliberately stops before manuscript prose. Live-verified across Dark/Mist/Abyss, no console errors. Open follow-ups (default theme choice, exact glow dial, Light's button hue) don't block calling this done. |
| **Story Labyrinth brand & type** | ✅ **BT0-BT4 all done (2026-08-04).** `docs/SL_Brand_Type_Implement_Design.md`. Kit: `docs/SL_Brand_And_Type.md`. Working name **Story Labyrinth** / SL — rename now shipped, see the SL0-SL3 row below. **BT0** - Inter/JetBrains Mono/Cormorant Garamond self-hosted woff2 (`public/fonts/`), `tailwind.config.js` `fontFamily.sans/mono/display`, `body` uses `font-sans`; manuscript canvas (`.ContentEditable__root`) explicitly kept off Inter (found and closed a real inheritance gap - it had no font-family of its own before this pass, so it would have silently gone Inter too). **BT1** - icon/favicon ladder + wordmark PNGs promoted into `public/icons`/`public/brand`; `index.html` favicon/apple-touch-icon + `manifest.json` icons wired (name strings updated to "Story Labyrinth" as part of the SL0-SL3 rename below, superseding T11's original "may stay Story Nexus until SL1" allowance). Transparent wordmark PNGs generated by chroma-keying the flat pinned exports (`sharp`, no Photoshop/psd-tools available in this environment - doc's own fallback clause covers this; a real PSD alpha export is a drop-in replacement at the same `public/brand/` paths whenever available) rather than blocking on T8's PSD-export step. **BT2** - TopBar shows the theme-aware wordmark (`isDarkThemeId()` in `theme-provider.tsx`, classified by each of the 18 themes' own `--background` lightness, not name - Graphite and the `mid-*` themes sort opposite to what their names suggest). **BT3** - `--brand-crimson`/`--brand-ember` tokens added once to the true `:root` (theme-independent, cascades under every theme pack); one restrained call site - a hover-only ember glow on the TopBar wordmark. Live hover-simulation-plus-computed-style check was inconclusive in this Browser-pane sandbox specifically (known automation quirk category, see `dev_environment_gotchas` memory) even though the compiled CSS rule, token values, and an identical inline-applied declaration all verified correct. **BT4** - one-line credit + freeware posture added to the Guide page's existing welcome header (no dedicated About page exists, this was the closest qualifying surface) - "Story Labyrinth" rendered in `font-display` (Cormorant Garamond), no existing strings changed. Does **not** reopen T1 chrome; no rename. See `DECISIONS.md`'s "Story Labyrinth Brand & Type - BT0-BT2" and "- BT3-BT4" entries. |
| **Story Labyrinth rename (SL0-SL3, SL5)** | ✅ **SL0-SL3 done (2026-08-04, same day as BT0-BT4), user-promoted.** Doc: `docs/SL_Brand_And_Type.md` §8. Product rename minimum (`package.json` name/displayName, README, app title/manifest, Guide/welcome strings, doc headers) executed on explicit user request ("since we are rebranding this whole app"). **SL1** — `package.json` (`name`/`displayName`) + `package-lock.json`'s two mirrored root-name entries; `index.html` `<title>`/apple-mobile-web-app-title; `public/manifest.json` `name`/`short_name`; README title/Overview (its "Road to Here" first-person history section left in Jon Silver's own voice, untouched — renaming his own retrospective narrative would misattribute a name he never used). **SL2** — Guide page title/welcome card (`GuidePage.tsx`, now correctly links `The Story Nexus` by name to the real upstream repo instead of the BT4 pass's now-superseded "evolving toward" phrasing); Login page password-protected string; document-title hook + its own template string; 5 Guide `.mdx` content files (basics/advanced/lorebook/prompts/tts). **SL3** — `CLAUDE.md`/`DECISIONS.md`/`CURRENT_BACKLOG.md` title headers; the `**Project:** Story Nexus Fork` header line repeated across ~10 individual design docs. **Deliberately NOT touched this pass** (SL5/SL6, or carries a real side effect — see `DECISIONS.md` for the full list): the SQLite DB filename/path (`storynexus.db` in `drizzle.config.ts`/`server/db/client.ts`/Docker files/deployment docs — renaming the default would make the app look for a file that doesn't exist on next restart), Docker image/service/container names + the GitHub Actions publish workflow + `.env.*.example` (deployment/publish identity), the session cookie name (`storynexus_session`) and `storyNexus.*` localStorage key prefixes (renaming would silently log users out / reset sidebar-collapse and session-summary state), `.git/config`'s remote URL (still points at the real upstream repo, a user action not a string edit), `UPSTREAM_CLAUDE.md` and every genuine upstream-credit reference (`JonSilver/TheStoryNexus` links/mentions), and historical/generated artifacts (`docs/archived/*`, `docs/design-mocks/*.html`, `.planning/codebase/*`). See `DECISIONS.md`'s "Story Labyrinth Rename — SL0-SL3" entry for the full trail. **SL5 done (2026-08-05)**, user-confirmed via `AskUserQuestion` on each risk point: DB filename renamed to `story-labyrinth.db` with a boot-time one-time auto-migrate (renames an existing `storynexus.db` + WAL/SHM sidecars in place, verified standalone in an isolated sandbox — not the live shared dev DB); Docker image renamed to `bloodgrv/story-labyrinth` (user's own namespace, confirmed they plan to actually publish to Docker Hub + Unraid + local build) across the publish workflow, all four compose files, both `.env.*.example` templates, and README/DEPLOYMENT docs; Tailscale hostname default renamed (template only, doesn't touch a live deployed `.env`); session cookie (`story_labyrinth_session`) and `storyLabyrinth.*` localStorage prefixes renamed (self-healing one-time logout/UI-state-reset, no data loss). SL4 (Hermes-side, outside this repo) and SL6 (own GitHub remote, user's own infra decision) still not done. See `DECISIONS.md`'s "Story Labyrinth Rename — SL5" entry. |
| **Transfer log + Settings IA** | ✅ **Done (2026-07-22).** Doc: `docs/Transfer_Log_And_Settings_IA_Design.md`. Settings reorganized into sub-nav headings (Appearance / Providers & keys / Local / Feature routing / Writing tools / **Logs** / Data — vertical Radix `Tabs`, `SettingsPage.tsx`). **Logs → Transfers**: new `deskTransfers` table (story-scoped send journal, desk→desk seeds only; `proposed`+`opened` events; 30d UI default / 90d hard delete via `pruneHistoryJob.ts`'s existing daily cadence); `TransfersLogCard.tsx` (story picker, search, Open origin, Re-seed destination); Recent Jobs moved under Logs alongside it. All slices **S0, T0–T3** shipped. Every writer lives client-side (a tray button, a fence-parse callback), so rows are created via a client `POST` rather than an internal service call — genuinely instrumented: Chat Shuttle propose/open/return, Brainstorm/Notes handoff propose/open, Outline lore-suggestion "Open in WB", and highlight→Notes-chat (new `highlight_to_notes` kind, not in the original design doc's enumerated mechanisms). Deliberately **not** logged (scoping call): same-desk sends (`note_split`, Notes' Import dump) and `overview_proposal`'s synopsis/memory sub-types (no desk destination, only the `note` sub-type counts). See `DECISIONS.md`'s "Transfer Log + Settings IA — S0, T0-T3, Load-Bearing Decisions" for the full trail, including why Re-seed needed client-side `navigate()` instead of a hard reload (the pending-seed fields are in-memory only) and why Open-origin stops at story+desk navigation rather than auto-selecting the specific chat. |
| **Lexical editor (deepen)** | ✅ **Plugin-add axis done (2026-07-22).** Doc: `docs/Lexical_Editor_Design.md`. Slices **LE0–LE3** all shipped: Lexical `0.39.0→0.48.0` bumped in lockstep across all packages; `ListPlugin`+`CheckListPlugin` mounted on the main Editor rich-text path (toolbar-driven bulleted/numbered/check lists verified live, correct `<ul>/<ol>/<li>` + `role="checkbox"` structure); Enter/Tab-in-list keyboard behavior not independently verifiable this session (browser-automation limitation, confirmed unrelated to lists — see `DECISIONS.md`). Export spot-check (LE3) found and fixed a real pre-existing gap: none of the three export converters (`convertLexicalToHtml.ts`/`convertLexicalToMarkdown.ts`/`lexicalUtils.ts`'s plain-text extractor, shared by PDF) had ever handled `list`/`listitem` nodes — latent since `ListPlugin` was never mounted before, but would have silently fused list-item text together on every export once LE1 made lists a normal editing action. Fixed all three (proper `<ul>/<ol>/<li>`, GFM `-`/`1.`/`- [x]` Markdown, per-item-line plain text for PDF). Version bump itself surfaced two real breaks, both fixed: `@lexical/react/LexicalContextMenuPlugin` was removed upstream in favor of `LexicalNodeContextMenuPlugin` (`ContextMenuPlugin/index.tsx` rewritten against the new API, verified against Lexical's current playground source); and `FloatingLinkEditor.tsx`'s update-listener returning `true` crashed chapter load outright under 0.48's new "listener return = cleanup fn" semantics (one-line fix). Tables stay locked **C** (nodes only, no `TablePlugin`, no insert UX) — untouched. CodeHighlight/TreeView/collab-residue, list "done" bar, and toolbar/mobile/selection/rework polish (scoped subset) all **shipped 2026-07-28** — see their own `DECISIONS.md` entries. The upgrade breakage/compat matrix **shipped 2026-07-28** too, closing all six T2 axes in full — see its own `DECISIONS.md` entry, including a real leaf-TextNode-subclass export bug the audit found and fixed (affected 3 of 4 export converters). See `DECISIONS.md`'s "Lexical Editor Deepen — LE0-LE3, Load-Bearing Decisions" entry, including a demo-data recovery incident during testing (recovered clean via the app's own chapter-snapshot restore). |
| **Amazon / KDP text standards** | ✅ **Done in full (2026-07-28), KDP0-KDP4.** Doc: `docs/Amazon_KDP_Export_Design.md`. New single Lexical→HTML SoT for the EPUB path (`src/utils/export/convertLexicalToEpubHtml.ts`, pure string-builder — the richer client `convertLexicalToHtml.ts` is DOM-based and can't run server-side, no jsdom dependency exists) replaces the old skinny server converter in `epubGenerator.ts`; fiction-core node mapping (bold/italic/underline/strikethrough/code, lists+checklist glyphs, quote, images, headings) with exotics (tables/code/collapsibles/layouts) collapsed to plain/stacked text. **Found and fixed a real correctness bug shared by both pre-existing HTML converters**: leaf `TextNode` subclasses with a non-`"text"` type (`HashtagNode`, this app's own `SpecialTextNode`) had their text silently dropped since neither converter recognized them — fixed by keying leaf-text detection off "has `text`, no `children`" instead of the exact type string (EPUB path only, per scope). Scene-break detection is new (empty paragraph / `***`/`* * *`/`---`-only paragraph → collapsed centered `* * *`, next paragraph gets no-indent) — a Lexical `horizontalrule` node is treated the same way (documented implementer call, doc only named the textual triggers). **Live verification caught a real bug in this first pass**: "paragraph has no text" was being used as the sole break trigger, which also matched a paragraph holding only an image (no caption) — silently dropping the image. Fixed by only treating truly-empty paragraphs (blank text/linebreak only) as breaks; a paragraph with an image is never a break regardless of its text. Front matter is now title+generated copyright only (synopsis removed from the body — was a real pre-existing lock violation); chapter titles fall back to `Chapter {order}` with no forced prefix; CSS's previously-dead `.chapter`/`.chapter-title` classes now actually apply, no paragraph margin (trade typography), no `font-family` declared (device default wins). **Found and fixed a real image-embedding bug as a prerequisite**: chapter images pasted/dropped into the editor are stored as base64 `data:` URIs, but `epub-gen-memory`'s `node-fetch`-based image fetch doesn't support `data:` URLs at all — fixed by writing each embedded image to a temp file and rewriting its `src` to a `file://` URL before generation. Soft preflight (`epubPreflight.ts` + `ConfirmDialog`, wired into `DownloadMenu.tsx` since chapter-level EPUB already delegates to the full-story export) warns on empty/short manuscripts, missing title/author, or huge images — never blocks, "Export anyway" always available. Guide: new "Exporting for Kindle" section in the Advanced Guide. Menu label now "Download as EPUB (Kindle)". See `DECISIONS.md`'s "Amazon/KDP Kindle-Ready EPUB Export — KDP1-KDP4" entry for the full trail. |
| **Context / token meter (local)** | ✅ **Done (2026-07-22).** Doc: `docs/Context_Token_Meter_Design.md`. Talk list **T4**. Composer chip (`ContextMeterChip.tsx`) shows a hybrid char-heuristic estimate of the full assembled prompt (reusing the chat's own `ChatContext`, not a second prompt builder) + expandable budget-slice breakdown; shown for Local always, hidden for other providers unless a post-turn usage is already known this session. Real usage capture (M3) scoped to **Local only** this pass (`stream_options.include_usage`, `streamUtils.ts`) — other providers stay in the design's own "usage not available → hidden" branch, not a cut corner; per-message usage badge persists via `ChatMessage.usage`. Local settings gain a context-window override + best-effort server-metadata fetch (`LocalAIProvider.fetchModels()` reads `loaded_context_length`/`max_context_length`/etc. when present) and a soft-warn confirm-before-send toggle (off by default, never a hard block). All slices **M0–M5** shipped. See `DECISIONS.md`'s "Context / Token Meter — M0-M5, Load-Bearing Decisions" for the full trail, including two automation-environment quirks hit during verification (client-side Vite HMR state corruption after many hot-updates, and a rounding-scale false alarm in chip reactivity testing). |
| **Chat shuttle (“agent chat shuffle”)** | ✅ **Done (2026-07-21).** Doc: `docs/Chat_Shuttle_Design.md`. All slices **H0–H7** shipped same day S0-S2 unblocked it. See `DECISIONS.md`'s "Chat Shuttle — H0-H7 — Load-Bearing Decisions" for the full trail. |
| **Import to Outline** | ✅ **Done (2026-07-24).** Doc: `docs/Outline_Import_Design.md`. All slices **OI0-OI8** shipped in one pass. New `outlineImportBatches`/`outlineImportChecklist` tables (dedicated, not a widened `brainstormChecklist` — see `DECISIONS.md`); new `outline_import` feature key; hybrid deterministic/LLM-normalize extraction (`outlineImportService.ts`, mirrors `documentImportService.ts`); Accept write path (`ai_suggested`+`confirmed`, 1-based order, never `chapterId`); Append/Replace-all (Replace wipes all outline rows+links, hard-confirmed); Outline panel draft+tray UI (`OutlineImportPanel.tsx`); Outline chat file-attach entry point + compact "Structure ready" card (`OutlineChatRail.tsx`/`OutlineImportCard.tsx`); post-Accept cast-link action (OI7, new `acceptedItemIds` column). See `DECISIONS.md`'s "Outline Import — OI0-OI8" entry for the full trail, including a real ordering bug caught and fixed pre-ship and a verification-process incident (demo story outline data was destroyed by Replace-mode testing and manually reconstructed — use a disposable test story for future accept/replace testing). |
| **Character Guided Playbook Packs (Hybrid D)** | ✅ **PP0-PP5 done (2026-07-24).** Doc: `docs/Character_Guided_Playbook_Packs_Design.md`. Reworked off the doc's original "Notes-backed" premise (didn't fit reality — `notes.storyId` is `NOT NULL`, no `metadata`/`folderId` column, no Notes-desk folder UI) onto a **dedicated `playbookPacks` table** + **dedicated Playbooks panel/tool** (not a Notes folder), per direct user conversation before building. Full 3-tier resolve ladder (story → global → shipped, `resolvePlaybookPack`), copy-on-edit for shipped/global packs, `.md`/`.txt` import with optional frontmatter, WB Character "Use playbook pack" arm toggle (alongside the existing psych-module toggle — `GuidedSetupControl`'s `extraToggle` generalized to `extraToggles`), direct context injection (new pattern — psych/place-sheet precedents turned out to be instruction-only, never actually reading their own metadata back into context), story-scoped export/import + a dedicated global export/import pair (mirrors Lorebook's). **PP6 (real interview-question prose) still deferred** — shipped packs ship as clearly-labeled placeholder shells until the user locks that content. See `DECISIONS.md`'s "Character Guided Playbook Packs — PP0-PP5" entry. |
| **Brainstorm / new-story import** | **Parked note 2026-07-20** (not designed). Sibling of Outline import, **not the same feature.** File as **head start** on creating a story — tray-heavy development via Brainstorm (and handoffs), little/no bulk spine commit. See parked section in `docs/Outline_Import_Design.md`. Do not fold into Import-to-Outline slices. |
| **Lore Sheet + Sync Loop (T5)** | **Design locked 2026-08-08.** Doc: `docs/Lore_Sheet_And_Sync_Design.md`. Talk list **T5**. **Job:** sheet-first SoT (`sheetBody` / **Lore Sheet**); structured fields are derived projection via **Sync** (hybrid deterministic `##` + LLM `sheet_sync`) → always propose→Accept. Description gets **narrative sections only**; Core Identity/Appearance → Codex. **All categories** with per-category section packs. Cross-desk: Maps **layout-brief pending card** (no silent scene write); Timeline **pending pins** for event/timeline; Notes **link only** (no body mirror). UI: sheet-first, **retire Natural View**; WB `sheet-proposal` + Accept & Sync. RAG: when sheet non-empty index `name+sheetBody+codex+edges` (skip description); placeState key lines de-duped into index. Migration lazy reverse-compile; doc import fills sheet+structured. Slices **FS0–FS8** (order FS0→FS1→FS2→FS3→FS4→FS6→FS5→FS7→FS8). **P3 — build in progress, user-promoted 2026-08-09 despite P3 label (started ahead of the "do not build until promoted" gate on explicit request).** **FS0 — ✅ Done (2026-08-09).** `sheetBody`/`sheetSyncedAt` nullable columns on `lorebookEntries` (migration `0062_long_joystick.sql`), client `LorebookEntry` type updated, CRUD pass-through (PUT already generic; POST create destructure updated), story export/import round-trip closed (same `updatedAt` JSON-timestamp bug pattern pre-empted for `sheetSyncedAt`). No UI/behavior change yet — schema + plumbing only. Live-verified against the real dev DB via direct authenticated API calls (GET/PUT/POST round-trip, existing rows read null, scratch data cleaned up). `npm run build` clean. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS0 (Schema)" entry. **FS1 — ✅ Done (2026-08-09), same day.** Per-category section packs (`sheetTemplates.ts`, all 8 categories per design §3/§4); new `LoreSheetEditor.tsx` (markdown + sticky section outline + "Insert template" + per-section "+ Add section") as the default primary editing surface; **Natural View retired in full** (`NaturalEntryView.tsx`/`NaturalStateFields.tsx`/`useNaturalEntryView.ts` deleted outright, not just unlinked); `AdvancedSettings.tsx` restructured to hold the machine chrome (`LevelScopeFields` + shrunk `RawEntryFields`: category/importance/tags/raw description/raw Codex state) as children, renamed "Advanced Settings" → "Advanced"; Name field + `OpenMapButton`/`PlaceOnTimelineButton` moved to the always-visible primary section (not machine chrome); seed-on-create + lazy-open-of-empty-sheet (required sections only via `buildEmptySheetSeed`, real reverse-compile is FS2 not this slice); a category-change reseed guard (only touches a still-untouched auto-seeded sheet, never real content). Live-verified end to end in the Browser pane against the real dev DB: seeded-template render, section insert + real save round-trip (reverted after), Advanced collapse contents, and a live category-switch reseed (Character → Location template swap, confirmed never persisted since not saved). `npm run build` clean. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS1 (Sheet Editor UI, Natural View Retired)" entry. **FS2 — ✅ Done (2026-08-09), same day.** `reverseCompileSheet.ts` — pure client-side, no AI — reverse-compiles an existing entry's `description`/`codexState`/`placeState` into its per-category sheet shape on lazy-open (replaces FS1's blank-template fallback for entries that predate this feature); wired for character (Codex) and location (placeState), the only two categories with real structured data today, every other category just lands `description` under its own catch-all heading. Optional "Improve with AI" (new `sheet_migrate` feature key, `sheetMigrateService.ts`, `POST /lorebook/sheet/improve`) mirrors `humanizerService.ts`'s one-shot rewrite pattern — stateless, not entryId-scoped, no propose/accept gate (the sheet is already a freely editable field; result lands in the unsaved form, Update is the real gate). Live-verified end to end in the Browser pane against the real dev DB: Elena Cross's real description correctly reverse-compiled under Personality & Temperament with Wardrobe/Wounds/Items correctly omitted (no source data); Berlin's description correctly landed under Overview; "Improve with AI" confirmed a real `POST` → graceful `{success:false}` (no AI provider reachable in this dev sandbox, same limitation nearly every AI-call feature here has hit) with zero console errors; confirmed nothing was persisted (read-only pass). `npm run build` clean. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS2 (Deterministic Reverse Compile + Optional Improve-with-AI)" entry. **FS3 — ✅ Done (2026-08-09), same day.** "Sync structured fields" button (`SheetSyncButton.tsx`) → `POST /lorebook/:id/sheet/sync` → `sheetSyncService.ts` (new `sheet_sync` feature key) → a `codexPendingChanges` row, reviewed via the *existing* Codex tray (`CodexPendingChangesPanel.tsx`, its render gate loosened from `codexEnabled` to just a saved `entryId` so it works for every category, not just Codex-enabled character/location entries). Hybrid parse: deterministic `##` split + line parsing always runs first; the `sheet_sync` LLM call only fires (once per click, batched) for sections with real prose content that didn't parse into anything structured. Structured (non-description) sync targets scoped to `character` (customFields/appearance/wardrobe/wounds/items) and `location` (customFields/items when `codexEnabled`, mirroring `placeCodexMapping.ts`'s existing chat-proposal mapping) — the only two categories with a live structured-state UI; the other 6 categories still get real narrative-section → description compile (a real bug in the first pass — those categories initially had zero sync targets wired at all — caught and fixed via a `SECTION_CONFIGS`/`allNarrative` lookup before shipping). Conflict rules (§6e) implemented exactly: labeled fields merge by case-insensitive label key against the entry's current saved state, list buckets full-replace, `proposedDescription` stays `undefined` (never `""`) when nothing narrative changed so Approve can never silently blank a real description. Live-verified end to end via three real scratch entries (character/location/item, created+deleted via API) covering first-sync, re-sync merge/replace semantics, and the narrative-only path, plus a real browser-UI pass against the live demo story (Berlin) confirming the tray renders and Reject leaves no residue. `npm run build` clean. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS3 (Sync to Codex)" entry. **FS4 — ✅ Done (2026-08-09), same day.** New ```sheet-proposal``` WB fence (`chatContextService.ts`'s `SHEET_PROPOSAL_INSTRUCTIONS`) — fires for **any** WB template once the chat is anchored to an entry (not template-gated like psych/place-sheet), injecting the anchor entry's category skeleton + current sheet content so the model can draft/expand it in place. `parseSheetProposal.ts` (raw markdown fence, not JSON — simplest parser in the whole propose/accept family) + `SheetProposalCard.tsx` (ephemeral accept/reject, mirrors Psych/PlaceSheet cards) + `handleAcceptSheet` (wholesale `sheetBody` replace) and `handleAcceptSheetAndSync` (chains the FS3 Sync call right after — a real two-gate convenience click, not a bypass: Sync's own output still lands as a separate Codex-tray proposal needing its own Approve) in `ChatInterface.tsx`. Live-verified the AI-independent half end to end (prompt injection with real category/current-sheet content, negative case for an unanchored chat, standalone parser regex test) — the actual model round-trip hits this project's usual "no reachable AI provider in this dev sandbox" limitation; the accept/sync write paths reuse FS2/FS3's already-verified endpoints unchanged. `npm run build` clean. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS4 (`sheet-proposal` WB Fence + Accept & Sync)" entry. **Real-provider follow-up (2026-08-09, same day):** the "no reachable AI provider" limitation FS2-FS4 hit turned out to be a false diagnosis, not an environment fact — `sheet_migrate`/`sheet_sync`/`worldbuilding_chat` had no per-feature endpoint override, so `aiClientFactory.ts`'s global-default fallback (fixed order local→openai→openrouter→grok, no reachability check) always picked local/LM Studio first and never reached the real, working OpenRouter key already in this DB. Fixed by setting explicit OpenRouter overrides for those three features (Settings → Feature Endpoints, no code change). With a real model reachable, did a genuine live end-to-end pass impossible in every prior session: real WB chat → real `sheet-proposal` fence → Accept & Sync → real `sheet_sync` LLM extraction → real Codex tray proposal, all against Elena Cross (reverted after). **Caught and fixed one real bug this surfaced**: the model's markdown-bold labels (`**Build:**`) broke `sheetSyncService.ts`'s bullet-strip regex, leaking stray asterisks into extracted field labels — fixed by stripping `**`/`__` emphasis globally and requiring real whitespace after a bullet marker. See `DECISIONS.md`'s "Root Cause: 'No AI Provider Configured' Errors Across FS0-FS4 Testing" entry and the updated `local_ai_endpoint_reachability` memory. **`aiClientFactory.ts` fix, user-requested same day:** `grok-oauth` — previously reachable only via an explicit per-feature override — added to the end of the global fallback chain itself (`local → openai → openrouter → grok → grok-oauth`), so any unconfigured feature can now reach a connected xAI OAuth session instead of dead-ending at "no provider configured." Verified via a throwaway script that temporarily knocked out local+openrouter config, confirmed the fallback resolved to a real `https://api.x.ai/v1` / `grok-4.5` client, then restored the DB row (confirmed via a fresh read). No other provider's fallback behavior changed. `npm run build` clean. **FS6 — ✅ Done (2026-08-09), same day.** RAG index changes in `ragIndexService.ts`'s `buildLorebookEntryText`: once `sheetBody` is non-empty, indexed text switches to `name + sheetBody + formatCodexState + placeState key lines (location only) + relationship edges` — `description` is skipped entirely (Sync already compiles the sheet's narrative sections back into `description`, so indexing both would mostly duplicate text); entries with no sheet keep indexing the legacy `name + description + codex + edges` shape unchanged. New `formatPlaceState` helper surfaces `entry.metadata.placeState` (scale/biome/holder/danger/landmarks/exits/floor) for locations — it deliberately omits `layoutMd`, since that's the same content the sheet's own "Layout Notes" heading already carries (via `reverseCompileSheet.ts` or direct user editing), so including it too would just duplicate that section verbatim — the de-dupe the design doc's own §7 table calls for. **Reindex hooks needed no new code**: `PUT /lorebook/:id` (the sheet-save path, used by both a manual Update and FS4's Accept/Accept & Sync) and `approvePendingChange` in `codexService.ts` (the Sync-Accept path, since `sheet_sync` proposals land as ordinary `codexPendingChanges` rows) already called `indexLorebookEntry` unconditionally on every applied write — both slice requirements were already satisfied by existing infrastructure, confirmed by tracing both call paths rather than assumed. `buildLorebookEntryText`'s signature is unchanged (still `(entry) => Promise<string>`), so `reconcileIndexJob.ts`'s staleness check keeps working with no changes of its own. `npx tsc --noEmit -p server/tsconfig.json` clean. Not independently live-verified against the running app this pass (server-side indexing-text-shape change, no UI surface to click through — verified by tracing both reindex call sites and the type-check instead). See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS6 (RAG Index Switch to Sheet-First Text)" entry. **FS5 — ✅ Done (2026-08-09), same day.** Cross-desk lanes added to `syncSheetToCodex`'s `SyncSheetResult`, each independent of the Codex-tray lane per §6d's "separate cards" split: **Map** (`mapLayoutBrief`, location only) — the sheet's "Layout Notes" content offered as a `metadata.placeState.layoutMd` merge via a new ephemeral `SheetSyncCrossDeskCard.tsx` "Apply to map" button, fires regardless of Place-Codex tracking (a separate field from `codexState`, nothing to conflict with), never touches Excalidraw `sceneJson` directly — only the pre-existing "Convert to sketch" banner can do that, still its own separate user action. **Timeline** (`timelinePinId`, event/timeline only) — reuses TL11B's existing `status:"pending"/source:"ai_suggested"` pin lane wholesale (`proposeAiSuggestedPin`, now accepting `linkType`/`linkId` so the pin links back to its source entry) rather than inventing a new review surface; reviewed via Timeline's own pre-existing Pending tab, a toast is all `SheetSyncButton.tsx` adds. New `findPinsByLink` de-dupes so re-sync never spams duplicate pins. **Notes** (`notesStub`, note only) — one-shot "Create note & open" (reuses `useCreateNoteMutation` + the existing `pendingNoteId`/`setCurrentTool` cross-tool nav pattern `OpenMapButton`/`PlaceOnTimelineButton` already established), no persisted link back (5c's "no dual-body mirror" — no `lorebookEntries`↔`notes` link column exists or was added). §4's `event`'s When/Where/Who→customFields target deliberately **not** built (same "no review surface" cut FS3's file header already made for every non-character/location category) — that raw text still feeds the pin's blurb/fuzzyPhrase directly without landing in any persisted field. **Found and fixed a real, genuinely dangerous bug during live verification**: `SheetSyncCrossDeskCard.tsx`'s four buttons had no explicit `type="button"` — since this card (unlike every prior proposal-card precedent, which render inside a chat transcript) renders *inside* the entry editor's real `<form>`, a bare `<button>` there defaults to `type="submit"`, so clicking "Dismiss" silently triggered the entry's real Update-submit handler underneath it. Caught live against the real demo entry **Berlin**: a scratch Layout-Notes edit used only to trigger the map-lane card got persisted to her real `sheetBody` twice before the fix, confirmed via network-request inspection and restored verbatim from a captured pre-edit copy afterward; re-verified post-fix (identical repro, zero `PUT` in the network log). All scratch entries/notes/the one scratch pending pin (rejected) cleaned up, confirmed via a final sweep. `npx tsc --noEmit` clean (server + client) throughout, including post-fix. See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS5 (Cross-Desk: Map Layout Brief, Timeline Pending Pins, Notes Stub)" entry for the full trail. **FS7 — ✅ Done (2026-08-09), same day.** Document-import drafts now seed a real, populated Lore Sheet instead of a blank template. `getDefaultFormValues` (`entryFormUtils.ts`) reverse-compiles a draft's already-extracted `description`/`codexState` through the same deterministic, no-AI compiler FS2 built for pre-existing entries (`reverseCompileSheet.ts`) — deliberately reused rather than adding a second LLM extraction pass (same "deterministic first, optional Improve-with-AI" doctrine, and the sheet's "Improve with AI" button is still there afterward). `reverseCompileSheet`'s parameter type narrowed from full `LorebookEntry` to a new `ReverseCompileSource` interface (just `category`/`description`/`codexState`/`metadata.placeState`, the only fields it reads) so a draft — which has no id/level/scopeId/etc. yet — can be passed directly; the one existing call site is unaffected since any real `LorebookEntry` still satisfies the narrower shape. No server change (`documentImportService.ts`/`DocumentImportDraft` untouched) — location-category drafts still leave placeState-only sections empty since the extraction schema never captured placeState to begin with, same "nothing to compile → empty heading" behavior as any other entry with no placeState. Live-verified end to end via a real `.txt` character-reference upload through the actual browser file-input flow against a real reachable AI provider (`document_import`'s existing `grok-oauth` override) — the resulting draft tab's Lore Sheet came back fully populated (Core Identity/Physical Appearance/Wardrobe/Personality & Temperament/Wounds/Items), with genuinely-empty sections (Background & Lifestyle, Character Motivations) correctly left blank rather than invented. Draft never saved, confirmed nothing persisted. `npx tsc --noEmit` clean (client + server). See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS7 (Document Import Writes sheetBody)" entry. **FS8 — ✅ Done (2026-08-09), same day. T5 (Lore Sheet + Sync Loop) is now fully shipped, FS0-FS8 all complete.** New shared `src/features/lorebook/utils/codexStateDiff.tsx` (`diffCodexList`/`diffCodexFields` + two renderer components) adds real diff-against-current-state rendering to **both** Codex-proposal review surfaces — `CodexPendingChangesPanel.tsx` (T5's own FS3 Sync-lane tray) and `ProposalTrayCard.tsx` (the older pre-T5 chat-proposal tray, `CodexProposalTray.tsx`) — since both show the exact same full-replace-list/merge-by-key-field proposal shape and showing a diff in only one would've been a real inconsistency. Wardrobe/wounds/items (full-replace, 6e) show kept/`+added`/~~removed~~; appearance/customFields (merge-by-key, 6e) show `before → after` or `(new)`, matching `sheetSyncService.ts`'s own merge logic exactly. **Soft notice (5d)** closes the one gap FS5 explicitly deferred: a global/series-level event/timeline entry with real When/Summary content now gets a `crossDeskNotice` sentence (plain `toast.info`, not an error) instead of the timeline lane just silently doing nothing. **Template polish**: `LoreSheetEditor.tsx`'s outline "scroll to heading" replaced its old proportional-by-line-count approximation (explicitly flagged as "FS8 territory" in FS1's own code comment) with a standard mirror-div pixel-accurate measurement, immune to being skewed by long wrapped paragraphs elsewhere in the document. Live-verified end to end against the real dev DB: a scratch codex-enabled character's Sync correctly rendered kept/added/removed wardrobe+items (colors confirmed via computed style) and changed/new/unchanged custom fields+appearance; a scratch global-level event entry's Sync returned the expected `crossDeskNotice` with no pin created; the scroll fix landed a heading at a proportionally-correct pixel position immune to a later wrapped-paragraph skew. All scratch data cleaned up. `npx tsc --noEmit` clean (client + server). See `DECISIONS.md`'s "Lore Sheet + Sync Loop — FS8 (Diff UX, Soft Notices, Template Polish)" entry for the full trail. Related: Codex tray, place sheet L0–L5, Maps v2, Story Timeline, document import. |
| **Lore Sheet inline rework with AI (T9, scoped, not built)** | **Parked/scoped 2026-08-09 — not designed for build yet.** Talk list **T9**. Doc: `docs/Lore_Sheet_Inline_Rework_Design.md`. Extends the existing description-field/chapter-prose "Rework in chat" sub-span pattern (`FocusTarget`/`pendingReworkStore`) to the Lore Sheet's `sheetBody` textarea — highlight a span, send to the entry's docked WB chat, Accept splices just that span back in — via a new `lorebook-sheet-field` focus-target variant and a new `sheet-span-proposal` fence, distinct from T5 FS4's whole-sheet `sheet-proposal`. Slices **IR0–IR6** proposed. Raised by the user while testing T5's sheet-proposal flow ("is the Lore Sheet inline-editable with AI?"). **Do not build until the user promotes it.** |
| **Chat chrome declutter — icon rail + drawers (T10, scoped, not built)** | **Parked/scoped 2026-08-09 — not designed for build yet.** Talk list **T10** (kept as its own item, explicitly not folded into T8, per user direction). Doc: `docs/Chat_Chrome_Declutter_Design.md`. Applies the chapter Editor's own right-rail pattern (`EditorToolsPanel.tsx` — icon column, one drawer open at a time) to the five other chat hosts (World-Building, Outline, Notes, Research, Brainstorm), which today stack chat list + Codex/approval tray + Context & memory toggles + (WB-only) playbook controls as always-visible siblings with no consistent chrome across hosts. Full host×bucket inventory + non-negotiable risks (chat list must stay non-modal so switching chats doesn't hide the transcript; pending-count badges and the Context & memory "armed" summary chip must survive collapse; WB's entry-scoped `CodexPendingChangesPanel` stays out of scope, it lives in the entry form not any chat) captured in the doc. Slices **CR0–CR8** proposed. Raised by the user directly comparing the Editor's rail favorably against the other chat surfaces' clutter. **Do not build until the user promotes it.** |
| **UX/UI polish cleanup (T8)** | **Parked 2026-08-09 — not designed.** Talk list **T8**. User wants a nicer look/feel while **keeping all tool sections/desks** (full toolbox, not a cull). Hierarchy, density, chrome quieting, polish. **≠ T1** (chrome tokens already shipped). **≠ T5** (sheet content model). Possible pilot: Lorebook after T5, or app-wide. **Do not build** until grilled + locked. |
| **Maps v2 — sketch maps (Excalidraw)** | ✅ **Fully done (2026-08-07) — MV0–MV7 all shipped, promoted and completed in one continuous build 2026-08-06/07.** Doc: `docs/Maps_V2_Sketch_Design.md` (implementation clarifications on thumbnail/layoutMd-convert/AI-fence-shape/sidebar-id locked same day). Talk list **Maps v2**. Kill sidebar Story Map **graph** UI; replace slot with **Maps** tool (**list → Excal canvas**). Hybrid ownership (free story maps **or** location-linked). SoT = Excal **scene JSON**; `layoutMd` export/import/legacy only; images still illustration. Relations stays for connections; no `storyMapEdges` rebuild v1. AI **in-app** propose→accept only (MCP later optional). Stack: `@excalidraw/excalidraw` MIT. Slices **MV0–MV7**. **MV0 — ✅ Done (2026-08-06).** New `storyMaps` table (title/storyId/optional locationId/sceneJson/thumbnailFilename, migration `0056_absent_photon.sql`), disk-stored thumbnail storage mirroring `lorebookImageStorage.ts`, full CRUD routes (`server/routes/storyMaps.ts`, bare `/api`, editor-level auth), location-delete cascade unlinks (not deletes) a map, story export/import round-trip (locationId remapped through the same idMap lorebookEntries import already builds; a map whose location didn't survive the export imports anyway as free-standing rather than being dropped). Live-verified end to end against the real dev DB — see `DECISIONS.md`'s "Maps v2 — MV0" entry for the full trail, including a known dev-environment gotcha hit and worked around (`tsx watch` under `concurrently` producing zero server output — used the `server-only`/`client-only` launch configs instead). **MV1 — ✅ Done (2026-08-06).** Sidebar `"story-map"` id kept, label/component swapped ("Maps", new `MapsTool.tsx`) — old React Flow graph tool (`StoryMapTool.tsx`/`StoryMapCanvas.tsx`) left on disk but unreferenced, confirmed genuinely unreachable via a Vite module-count drop in `npm run build`. New `features/story-maps/` (plural, deliberately verbose naming to stay unambiguous next to the old singular `features/story-map/`) — list/empty state, title-only create dialog (location-linking UI is MV3, not pulled forward even though MV0's backend already supports it), detail shell with inline rename + delete, MV2-placeholder canvas area. Live-verified end to end in the Browser pane — see `DECISIONS.md`'s "Maps v2 — MV1" entry. **MV2 — ✅ Done (2026-08-06).** Real `@excalidraw/excalidraw` (0.18.1, MIT) embed replacing the MV1 placeholder, lazy-loaded via `React.lazy`/`Suspense` (large per-locale font-subsetting asset set, correctly code-split away from the main bundle). Load/save via `serializeAsJSON`'s canonical `.excalidraw` shape; theme follows the app's active theme live; native Excalidraw Undo/Redo, no custom undo code. **Found and fixed a real bug live during verification**: Excalidraw's own idle/presence heartbeat fires `onChange` every ~2s even with zero edits, spaced further apart than the 1s save debounce, so every idle pulse independently fired a real save (21 PATCH calls for one drag) — fixed by gating saves on `getSceneVersion(elements)` so appState-only churn never touches the debounce timer at all; re-verified 10s fully idle now produces zero network calls. Also fixed a title-rename race the new autosave would otherwise have introduced (re-keyed a sync effect from the whole `map` object to `map?.id`). Live-verified end to end (draw → reload → persisted; dark/light theme; undo/redo) — see `DECISIONS.md`'s "Maps v2 — MV2" entry. **MV3 — ✅ Done (2026-08-06).** Both remaining create-flow entry points: `NewMapDialog.tsx` gained an optional "Link to a location" picker (all of a story's location entries, via a new `useLocationEntriesQuery`); a location lore entry's Form View (`RawEntryFields.tsx`) gained an `OpenMapButton.tsx` — "Create map for this location" / "Open map" depending on whether one already exists (`GET .../maps/by-location/:locationId`), navigating to the Maps tool via a new one-shot `StoryContext.pendingMapId` pointer (mirrors `pendingLorebookEntryId`'s pattern). `MapsListPanel.tsx` now resolves real location names for its badges instead of a generic "Location-linked" label. **Found and flagged, not fixed (out of Maps v2's scope)**: Natural View (the default-on entry presentation) renders no location-specific content at all, not even the pre-existing place-sheet fields — a user has to switch to Form View to see the Open Map button or place sheet, a pre-existing gap this slice didn't create. Live-verified end to end (create-from-location, re-open reuses the same map without duplicating, location-link dialog, resolved list badges) — see `DECISIONS.md`'s "Maps v2 — MV3" entry. **MV4 — ✅ Done (2026-08-06).** Text bridge both directions. Export: new `sceneToMarkdown` (`features/story-maps/lib/sceneToText.ts`, no Excalidraw import — reads plain scene JSON) summarizes the last-saved scene; a location-linked map's "Export to place sheet" button replaces `metadata.placeState.layoutMd` (reuses the exact merge pattern `ChatInterface.tsx`'s place-sheet-proposal accept already established); a free map's "Copy as text" writes to the clipboard instead; an empty scene shows an info toast rather than exporting nothing. Import: a location-linked map with an empty scene and non-empty `layoutMd` shows a one-time "Convert to sketch" banner (gated on scene-emptiness, not a new persisted flag — self-closing the moment any element exists) — accepting seeds a single text element via `convertToExcalidrawElements` (kept inside `MapCanvas.tsx`'s lazy chunk, not the eager `MapDetailPanel.tsx`) and persists it immediately, not on the next autosave tick. Live-verified end to end (convert → export round-trip, free-map clipboard path, empty-scene guard) — see `DECISIONS.md`'s "Maps v2 — MV4" entry. **MV5 — ✅ Done (2026-08-07).** AI propose→accept, full-scene-replace only (per-element patch/merge explicitly deferred). Host: the location entry's own WB chat (Locations template only, same unconditional gating precedent as `place-sheet-proposal`, no new "Maps chat" type — free/unlinked maps have no AI-propose path in v1, a disclosed scope cut). Model emits an Excalidraw Element Skeleton directly (`MAP_SKETCH_INSTRUCTIONS` in `chatContextService.ts`); `parseMapSketchProposal.ts` + `MapSketchProposalCard.tsx` mirror the existing `psych-proposal` wiring exactly. Accept never imports `@excalidraw/excalidraw` outside `MapCanvas.tsx`'s lazy chunk — `handleAcceptMapSketch` (ChatInterface.tsx) resolves/creates the anchor location's map, then hands the raw unconverted skeleton to a new one-shot `StoryContext.pendingMapSketch` pointer; `MapCanvas.tsx`'s new `seedSkeleton` prop (full replace, no confirm dialog — matches every other propose→accept flow's no-second-confirm precedent) is where `convertToExcalidrawElements` actually runs, reusing MV4's "seed via mount-time initialData" trick. **Live-verified end to end against a real reachable cloud model for the first time in this project's propose→accept history** (every prior slice had no AI provider reachable in-session) — real model reply → correct fence → proposal card → Accept → new map created, linked, navigated to, and rendered with the exact elements described. See `DECISIONS.md`'s "Maps v2 — MV5" entry. **MV6 — ✅ Done (2026-08-07).** PNG-only export (SVG dropped — doc treats PNG/SVG as either/or, PNG alone covers the stated use case) via Excalidraw's own `exportToBlob` — sidesteps the old L3 Story Map's `html-to-image` DOM-screenshot approach and its documented hang/timeout-race quirk entirely, since `exportToBlob` operates directly on the elements array. Dynamically imported inside `MapDetailPanel.tsx`'s click handler (not a static import) so the button doesn't pull `@excalidraw/excalidraw` out of `MapCanvas.tsx`'s lazy chunk. Guide (`locations-maps.mdx` §3) rewritten in full — replaced every stale L3 graph-tool reference (edge types, "Add map link," etc.) with the real MV0-MV5 feature set including a new "AI-sketched maps" subsection; `settings-nav.mdx`'s sidebar copy fixed too (still said "Story Map"). Live-verified (empty-scene guard, real PNG blob+download attempt via a monkey-patched anchor click, Guide page rendering with no MDX errors) — see `DECISIONS.md`'s "Maps v2 — MV6" entry. **MV7 — ✅ Done (2026-08-07), closing out Maps v2 in full (MV0–MV7 all shipped).** Confirmed "hide edges API from UI" was already true since MV1 (a full-project grep found zero live imports of `features/story-map/` outside its own directory — `StoryMapTool.tsx`/`StoryMapCanvas.tsx` are genuinely unreachable, not just unrendered). Marked `storyMapEdges`/`storyMapLayout` (schema.ts) and the `storyMap.ts` route file as soft-deprecated via code comments (no runtime change — routes and data stay live, since story export/import for old backups still round-trips through them). **DB drop deliberately not done** — the design doc's own "optional... later" framing, and dropping data is a one-way door not worth taking without a specific reason to. Design doc's own status header and phasing/acceptance-checklist tables updated to reflect full completion. Pure comment/doc changes this slice — no behavior to live-verify, confirmed via `npm run build` (tsc client+server+vite) clean with identical output to the prior slice (no functional change). See `DECISIONS.md`'s "Maps v2 — MV7" entry.
| **Story Timeline (T6)** | ✅ **TL0-TL12 done in full (2026-08-07)**, user-scoped via `AskUserQuestion` across four build passes from the full 13-slice fat design. Doc: `docs/Story_Timeline_Design.md`. Talk list **T6**. In-world chronology board (time twin of place Maps), modeled closely on Maps v2's shape. **TL0** — `storyTimelines`/`storyTimelinePins`/`storyTimelineMemberships` schema; spine is **lazy get-or-create** (`ensureSpineTimeline`) rather than hooked to story creation — covers pre-existing stories too. **TL1** — tool shell (`src/features/story-timeline/`, sidebar peer of Maps), board list/CRUD pins, Story-start anchor (Chapter One default, manual override via `StoryStartControl.tsx`). **TL2** — time model (relative/fuzzy/civil) + a strict 3-tier sort pipeline (`sortPins.ts`). **TL3** — "Place on timeline" (`PlaceOnTimelineButton.tsx`) wired into Lorebook (any category), Chapters, Notes; "open link" navigation reuses `StoryContext` pointers plus a new `pendingNoteId`. **TL4** — H\|V toggle (per-timeline) + Story-start marker chrome. **TL5** — named timelines + board switcher (`TimelineSwitcher.tsx`: create/rename/delete), multi-timeline membership (`PinMembershipPopover.tsx` — one pin, checkboxes per timeline, never a duplicate row); deleting a named timeline **auto-preserves** any pin exclusive to it onto Spine rather than orphaning it; `removeMembership` blocks removing a pin's last remaining membership (400, "delete the pin instead"). **TL6** — swimlanes (`storyTimelines.swimlanesEnabled` column, no separate lanes table — `laneId` is a free-text field set per-membership in the same popover), lane rows sorted alphabetically with "Unassigned" trailing, H\|V toggle hidden while swimlanes are on. **TL7** — AI pin proposal fence on the WB `timeline` template (`TIMELINE_PIN_INSTRUCTIONS`, mirrors `psych-proposal` exactly), a JSON **array** per reply (not a single object — multiple beats per turn), native-pins-only (no chapter/lorebook/note links — the template isn't entry-anchored), `TimelinePinProposalCard.tsx` with per-item Accept/Reject + Accept all, Accept reuses the existing `createPin` mutation unchanged (defaults to Spine). **TL8** — opt-in compact chronology block (`aiChats.includeTimeline`, mirrors `includeMemory` exactly), Spine-only, `{title, blurb, when}` per pin — never full linked bodies — assembled into a `[STORY TIMELINE — established chronology]` labeled block in `ChatInterface.tsx`. **TL9** — "era/overview strip" scoped down to a compact tick strip (`TimelineOverviewStrip.tsx`, no new "era" entity/table — out of proportion for a polish-tier slice), grouped by the same 3 sort tiers, click-to-scroll via a `data-pin-id` attribute on `PinCard.tsx`. **TL10** — "Export as image" (`TimelineBoard.tsx`'s `handleExportImage`), mirrors Story Map's own `toPng`/timeout-race pattern (`html-to-image`, already a dependency) but simpler — a plain scrolling DOM board needs no `getNodesBounds`/`getViewportForBounds` math, just `toPng` against a ref'd container. **TL11A** — scanner chronology-aware check: extracted `resolveTimelinePins` out of `chatContextService.ts` into a shared `storyTimelineService.getSpineChronologyExcerpt` (now used by both TL8's chat context and this), threaded `includeTimeline` through `runChapterScan`/`scanChapter`/`scanStory` exactly like `includeMemory`, new toggle in both `ChapterScannerDrawer.tsx` and `RagScannerPanel.tsx`. **TL11B** — bulk-suggest AI pin proposals: new `status`/`source` columns on `storyTimelinePins` (mirrors `storyGraphEdges`), `timeline_suggest_pins` job (mirrors `graphSuggestEdgesJob.ts` exactly — manual-trigger-only, reads lorebook entries + notes, writes `pending`/`ai_suggested` rows), new Pending tab (`PendingPinsPanel.tsx`) + owner-gated "Suggest pins" button in `TimelineTool.tsx`, approve/reject routes mirroring the Relationship Graph's own. **TL12** — new `story-timeline.mdx` Guide page registered in `GuideTabs.tsx` right after Locations & Maps (its natural neighbor); polish pass found no rough edges needing fixes. Live-verified end to end against the real dev DB: overview strip renders/scrolls, export button fires with no console errors, the `timeline_suggest_pins` job runs the full pipeline through to a real (expected, no-provider-configured) connection-error, the scanner's `includeTimeline` payload threads through to a real job run, the approve route correctly 400s a non-pending pin, and the Guide tab renders its content. All test pins cleaned up afterward. Several real bugs found and fixed across all four passes this feature took (dnd-kit swallowing card clicks; native `confirm()` replaced with `ConfirmDialog`; a real pre-existing allowlist gap in `PATCH /api/chats/:id`/`updateMeta`; a stale dev server process serving pre-edit code, caught by an API response missing fields that should have been there) — see `DECISIONS.md`'s "Story Timeline — TL0-TL4", "— TL5-TL6", "— TL7-TL8", and "— TL9-TL12" entries for the full trail. Keep lorebook `event`/`timeline` as content types (unchanged, untouched by this feature). |
| **Name generator** | ✅ **Core shipped (2026-07-22, NG0-NG7).** Design: `docs/Name_Generator_Design.md` v0.4. Committed as `6364c08`. ✅ **Region packs shipped in full (2026-07-24 NP0/NP1/NP2/NP4/NP5, 2026-07-27 NP3)** — `docs/Name_Generator_Region_Packs_Design.md`. All 24 packs vendored into `server/data/name-packs/` + `manifest.json`; `GET /api/name-generator/packs`, `POST .../:packId/install` (idempotent, deterministic `pack:{id}:{slug}` pool ids, `source: "pack"`), `POST .../:packId/uninstall`; Name Generator → Import → **Browse packs** tab (quality badge, Install/Uninstall, defaults to global scope) plus **NP3's 5 bulk presets** (European/MENA/Asian/African/Anglo-Americas, client-side grouping covering all 24 packIds, `useInstallPresetMutation` fans out to the existing per-pack install endpoint via `Promise.allSettled` + one aggregate toast). Live-verified: install/idempotent-reinstall/uninstall round-trip, Region filter picking up new packs, MENA preset installing all 4 packs (16 pools/7008 names) in one click and correctly flipping to a disabled "installed" state. |
| **Locations & maps** | ✅ **L0-L3 done (2026-07-22).** Design: `docs/Locations_And_Maps_Design.md`. **L0** — WB `locations` template's `systemPromptHint` walks the 10-slot location script (prompt-driven, not a tracked state machine, same doctrine as Brainstorm/WB style depth); new `PLACE_SHEET_INSTRUCTIONS` always-on when `templateSlug === "locations"` (no opt-in toggle, unlike psych — place-sheet fields are the template's whole point). **L1** — `entry.metadata.placeState` (scale/biome/holder/dangerLevel/landmarks/exitsSummary/layoutMd/imageBrief), same posture as the Character psych module but with a real manual-edit form (`PlaceSheetFields.tsx`, category="location" only) alongside chat propose→accept (new `place-sheet-proposal` fence, `PlaceSheetProposalCard.tsx`, `ChatInterface.tsx`'s `handleAcceptPlaceSheet` merges into `metadata.placeState` via the existing generic lorebook update route, mirrors `handleAcceptPsych` exactly). **Found and fixed a real, previously-live bug as a prerequisite**: `entryFormUtils.ts`'s `buildSubmitData` hardcoded `metadata: {importance, status, type, relationships: []}` on every normal entry save, wholesale-replacing the metadata column (the generic PUT route doesn't merge) — silently wiping `psychProfile` and any other extra metadata key on every plain "edit description, hit Save," not just this pass's new `placeState`. Fixed by spreading `...entry?.metadata` first; live-verified the fix directly (Berlin's pre-existing `customFields` survived a save that also wrote new `placeState`). **L2** — `generateLorebookImage(entryId, preset)` in `grokImageService.ts`, `"map"` preset prefixes a dedicated top-down/ink framing and pulls from `placeState.imageBrief`/`layoutMd`/`landmarks` (fallback chain) instead of the raw description; Mood\|Map toggle in `ImageUploadField.tsx`, shown only for category="location". **L3** — new Story Map tool (`src/features/story-map/`, mirrors `src/features/story-graph/` closely): `storyMapEdges`/`storyMapLayout` tables (migration `0050_dusty_nemesis.sql`), own `StoryMapEdgeType` enum (`contains`/`borders`/`road_to`/`portal_to`/`below`/`above`/`other` — deliberately separate from the Relationship Graph's 15-value factual allowlist per CLAUDE.md's own lock), `storyMapService.ts`/`storyMap.ts` routes (mounted bare `/api`, same reasoning as `storyGraph.ts`), single-canvas React Flow UI (no Ego/Full split — one "Focus on region" dropdown that walks `contains` edges instead) with drag-persisted layout, node click → side panel → Open entry/Add map link, entry delete cascades to map edges+layout. **Scope cuts, matching how the Relationship Graph itself staged G1 before G1.5+**: manual-CRUD-only this pass (no pending/AI-suggest lane, no `status`/`source` columns), no WB-chat-driven map node/edge proposals yet. Live-verified in the Browser pane: Story Map renders the demo story's 3 location entries (Berlin/Prague/Moscow), created a real `contains` edge end-to-end (side panel Outgoing count updated), layout position PUT/GET round-tripped, Place sheet form fields render/save/reload correctly, Mood\|Map toggle shows only for locations. The `place-sheet-proposal` fence parse→card→accept path itself could only be verified by code-mirroring against the already-shipped psych-proposal pattern (identical shape) — same "no reachable AI provider in this dev sandbox" limitation every prior P0.3/P0.4 session has hit, and manually-inserted historical messages don't retroactively parse (proposals are parsed client-side only at generation time, by design, same as every other proposal fence in this app). All test entries/edges/layout/chats cleaned up from the demo story afterward. See `DECISIONS.md`'s "Locations & Maps — L0-L3" entry. ✅ **L4-L5 done (2026-07-23)**, confirmed scope via `AskUserQuestion`: build L4; L5 = multi-floor/sub-POI nesting + export, travel-time cut (zero precedent anywhere, lowest value of L5's three bundled ideas). **L4** — the Character Codex versioning machinery (`codexEnabled`/`codexState`/`codexSnapshots`/`codexPendingChanges`) turned out to already be category-agnostic (gated only on `codexEnabled`, not category) — zero schema changes needed. Two-tier model: `PlaceSheetFields.tsx` (L1, unversioned) until a location's "Track Place State" switch (new `PlaceCodexStateEditor.tsx`, mirrors `CodexStateEditor.tsx` — `LabeledFieldsBox`/`StateListBox` extracted into shared `codexStateBoxes.tsx`) is flipped on, one-time-prefilling `codexState.customFields`/`items` from the existing `placeState`; `place-sheet-proposal` Accept then branches on `codexEnabled` to route through the real `codexPendingChanges` propose/approve/reject/Edit-First pipeline (`ChatInterface.tsx`'s `handleAcceptPlaceSheet`) instead of a direct metadata merge — reusing the exact mutation the `codex-proposal` fence pathway already uses, so `CodexProposalTray.tsx`/`CodexPendingChangesPanel.tsx`/`CodexHistoryPanel.tsx` all work unmodified. **Live-caught a real wiring bug before it shipped**: `PlaceCodexStateEditor` was initially gated behind `codexEnabled` itself, making its own "Track Place State" switch unreachable — fixed to render unconditionally for locations, matching `CodexStateEditor`'s own always-rendered pattern. **L5a** — `floorLabel` (free text) added to `PlaceState`/place-Codex; Story Map nodes show a floor badge and region-focus view sorts numeric-prefix-aware within a `contains`-nested region, no new table. **L5b** — Story Map data now round-trips through story JSON export/import (`fromId`/`toId`/`nodeId` remapped through the same `idMap` lorebook entries already build, first graph-shaped data to ever do so — `storyGraphEdges` deliberately left out for now); "Export as image" button (new `html-to-image` dependency, React Flow's own documented pattern, not `html2canvas` which is only an indirect `jsPDF` dependency). **Found a real environment limitation, not an app bug**: `toPng` hung indefinitely in this session's browser-automation sandbox on any element at all (even a bare `<button>`), so a 15s client-side timeout was added as a permanent safety net regardless of root cause — confirmed the button correctly re-enables and shows an error toast on timeout. Live-verified end to end: L4's full toggle→prefill→save→snapshot→propose→approve round-trip via direct API calls mirroring the real UI flow; L5a's floor badge and region-focus rendering; L5b's export/import id-remap (confirmed no stale ids survived) and the image-export timeout's graceful recovery. All test data (entries' metadata, a scratch chat, a scratch map edge, an imported test story) cleaned up afterward. See `DECISIONS.md`'s "Locations & Maps — L4-L5" entry. |
| **Scene Beat removal** | ✅ **Done in full (2026-07-22).** Doc: `docs/Scene_Beat_Removal_Design.md`. Removed inline Scene Beat generators in full: Alt+S, Insert/Slash menu entries, `SceneBeatNode` + whole `nodes/scene-beat/` tree, `/api/scenebeats` + client hooks, story/series/admin export/import, demo seed. **Concrete Beats untouched and confirmed still working.** Content migration (`sceneBeatContentMigration.ts`) rewrites any `scene-beat` Lexical node into a plain paragraph with its command text — live-verified against the real dev DB before the table drop. `promptType: "scene_beat"` rows recategorized to `"other"` via a boot-time migration (not deleted) — a real existing system row was live-migrated and confirmed. **SB6 (drop the `sceneBeats` table) done** — user confirmed (via `AskUserQuestion`) no other deployment has real Scene Beat data; migration `0047_misty_tempest.sql` applied and verified. See `DECISIONS.md`'s "Scene Beat Removal — SB1-SB8" entry. |
| **Chat model routing + chrome density** | ✅ **Done (2026-07-22).** Doc: `docs/Chat_Model_Routing_And_Chrome_Design.md`. Settings preferred **Cloud\|Local** mode (new `aiSettings.preferredMode`, "Chat default routing" card on Providers & keys); `resolveChatDefaultModel`/`resolveModelForModeSwitch` (`src/features/ai/utils/resolveChatDefaultModel.ts`) auto-pick a default so new chats are never modelless; chat control = Cloud\|Local segmented pill + mode-filtered/provider-grouped model menu (`ChatSystemPromptControl.tsx`), Edit system prompt moved to a "⋯" overflow menu. **Context & memory** toggle groups collapsed by default behind one `Collapsible` (armed-only summary chip when collapsed, global localStorage state). **Compact** single-line `ChatListItem` rows (short timestamp, hover-only edit/delete) — applies to all five rails sharing `ChatList`/`ChatListItem`. All slices **MR0–MR2, CC0, CL0** shipped. See `DECISIONS.md`'s "Chat Model Routing + Chat Chrome Density — MR0-MR2, CC0, CL0" entry. |
| **Local in-process embeddings (drop LM Studio dependency for RAG embeddings)** | ✅ **Done (2026-07-22).** Doc: `docs/Local_Embeddings_Design.md`. Slices **IE0–IE6** shipped same day. New provider `"local-inprocess"` on the `"embedding"` feature only (`@huggingface/transformers`, `nomic-ai/nomic-embed-text-v1.5`, `dtype: "q8"`, 768-dim — matches `EMBEDDING_DIMENSIONS`, **no `vec_chunks` migration needed**). One correction from this row's original framing: `onnxruntime-node` (the actual Node backend) ships **prebuilt native binaries**, not "no native bindings" — same risk class as `sqlite-vec`'s prebuilt binary, needs `libgomp1` at runtime (added to both Dockerfile stages). Model weights are **baked into the Docker image at build time** (`server/scripts/prefetchEmbeddingModel.mjs`, builder-stage-only network fetch) rather than downloaded at first run — runtime sets `env.allowRemoteModels = false` unconditionally, verified via a new `verify-local-embeddings.mjs` sanity script (both stages, mirrors the `verify-sqlite-vec.mjs` precedent). `embeddingService.ts`'s `embedTexts()` branches to the new `localEmbeddingService.ts` before ever touching `aiClientFactory.ts`/OpenAI — no HTTP client exists for this provider, enforced both in the Settings UI (`FeatureEndpointsCard.tsx`, option only shown on the Embeddings row) and server-side (`routes/admin.ts`, rejects the provider on any other feature key). Closed a real correctness gap this feature would otherwise ship with: `reconcileIndexJob.ts`'s staleness check now also treats a chunk's `embeddingModel` not matching the currently-configured model as drift (across **all five** embeddable entity types — memory/note/outline chunks were previously never staleness-checked at all, orphan-detection only), and the same job type now accepts `job.storyId === null` to mean "run across every story" (mirrors `prune_history`'s existing global-job precedent — no new job type). New "Rebuild embedding index" button + confirm dialog next to the Feature Endpoints card triggers this. Live-verified end to end in the Browser pane and via direct DB/script checks: saved the provider, ran the rebuild job, confirmed all 18 lorebook entries + 3 chapters + 1 note + 1 outline item in the dev story picked up real 768-dim vectors tagged `nomic-embed-text-v1.5 (local)` (`vec_chunks` embedding length 3072 bytes = 768 × float32, not a placeholder), and a real `hybridSearch` query returned topically correct, semantically-matched results. Confirmed the `env.allowRemoteModels = false` path resolves purely from the pre-populated cache with zero network calls. **Docker build verified once Docker Desktop was started, same session:** full `docker build` succeeded both stages, and `docker run --rm --network none ...` proved the embedding pipeline works with the container's networking fully disabled — the strongest possible confirmation of the "no runtime network access" design decision. Test image/container removed after verification. See `DECISIONS.md`'s "Local In-Process Embeddings — IE0-IE6" entry. |
| **Dep majors (pack 3) — todo later** | **Parked 2026-07-21; ROI freeze 2026-07-27** (talk-list lock). **Low product ROI** — hygiene/risk only, not a feature mission. Stack is already current enough on the load-bearing bits (React 19, Express 5, Zod 4, Lexical 0.48, Vite 7, better-sqlite3 12.x). **Do not promote the whole pack.** Open **one family per PR** only if: (1) forced by CVE/node/upstream drop, (2) a concrete bug maps to that package, or (3) deliberate boredom maintenance with no feature pressure. **Prefer when opening:** **3a** stay on `better-sqlite3` **12.x** latest + native/Docker/`sqlite-vec` smoke (**defer 13**); **3i** knip/concurrently/`@types/*` minors anytime; **3b** `jspdf` 3→4 only if PDF export actually hurts; **3c** lucide 1 only if accepting a noisy rename pass for cosmetics. **Hard park / never drive-by:** **3g** Tailwind **4** (theme rewrite project — dedicated only), **3e** `react-resizable-panels` 4 (shell sacred), **3d** `react-router` 8, **3f** Vite 8, **3h** TypeScript 7 last/never-unless-forced. Suggested historical order if ever forced to sequence: 3a → 3b → 3c → 3d *or* 3e → 3f → 3g → 3h → 3i. **Do not** couple with Lexical LE* or feature builds. See `docs/SN_Planning_Talk_List.md`. |
| Spellcheck / LanguageTool depth | Settings/types exist; full design may exceed current UX |
| Gemini provider polish | `docs/gemini-provider-plan.md` |
| Mobile responsive overhaul | `docs/mobile-responsive-refactor-plan.md`, issue-58 plan |
| Obsidian integration | Hermes design only |
| Full multi-writer collab (invites, comments, change tracking) | Beyond basic owner/editor/viewer patterns |
| **Advanced export profiles** | Baseline export exists. **Separate from T3** — T3 locks Kindle-ready rules on the single EPUB path; a future profile system may reuse those rules as a “Kindle” profile without T3 shipping profile SoT/UI. |
| Style memory / what-if explorer / alt generations | Old Phase 2 roadmap |
| Analytics, plugin system | Old Phase 5 |
| Personal_Agent_Memory → Story Labyrinth bridge | Separate product; API client later, not a fork rewrite |
| Graph/memory psych or power-dynamic types | **Out of scope** permanently unless user reopens |

---

## Explicitly out of scope (unless user reopens)

- Thematic / corruption / psychological enforcement pipelines as system features
- External queue/worker (Redis, Bull) or second process for jobs
- Neo4j / Mem0 / Letta inside Story Labyrinth
- Merging Personal_Agent_Memory_System into the fork DB

---

## Suggested next Claude kickoff (copy-paste)

```text
Read CLAUDE.md, docs/CURRENT_BACKLOG.md, docs/SL_Brand_And_Type.md,
and docs/SL_Brand_Type_Implement_Design.md.

Implement Story Labyrinth brand & type slices BT0 → BT1 → BT2 only
(BT3/BT4 optional if trivial).

BT0 (required): Self-host Inter + JetBrains Mono + Cormorant Garamond;
Tailwind fontFamily sans/mono/display; app shell menus/chrome use Inter.
Do NOT change Lexical/manuscript editor prose fonts.
Do NOT reopen UI_Visual_Direction / T1 chrome tokens.
Do NOT rename package.json / window title / README to Story Labyrinth (SL0+ later).

BT1: Promote pinned icons/favicons from
docs/design-mocks/brand/exports/ into public/icons (wordmarks → public/brand
when transparent PNGs available). Wire favicon + manifest icons.
Manifest name strings may stay "Story Nexus" until SL1.

BT2: TopBar wordmark from pinned dark3/light2 assets.
PSD SoT for transparent: SL_wordmark_live_dark3.psd / SL_wordmark_live_light2.psd.

Record decisions in DECISIONS.md; update CURRENT_BACKLOG.md and the implement
design doc slice checkmarks when done. npm run build must stay clean.
```

### Alternate kickoff (if not promoting brand)

```text
Read CLAUDE.md and docs/CURRENT_BACKLOG.md.
P0–P2 foundation work is largely done. Remaining open P3 includes PP6 starter
content and items marked not started. Promote one locked design explicitly.
Record load-bearing decisions in DECISIONS.md; update CURRENT_BACKLOG.md when done.
```

---

## Document map

| Doc | Role |
|-----|------|
| `CLAUDE.md` | Architecture + constraints + high-level done flags |
| `DECISIONS.md` | Why/how of each shipped change |
| `docs/CURRENT_BACKLOG.md` | **This file** — remaining work + priority |
| `docs/Notes_Outline_Chat_Bridges_Design.md` | Notes/Outline ↔ chat double-gate + export packaging (locked 2026-07-18) |
| `docs/SN_Planning_Talk_List.md` | Queued grill topics (UI, Lexical, Amazon/KDP, context display, …) |
| `docs/Amazon_KDP_Export_Design.md` | T3 Kindle-ready EPUB (**locked/shipped** 2026-07-28); KDP0–KDP4 done — see `DECISIONS.md` |
| `docs/Chat_Panel_Integrations_Design.md` | WB/Editor locks, selection rework, generalized panel↔chat pattern |
| `docs/Character_Guided_Playbook_Packs_Design.md` | Character Guided setup Hybrid D packs (**mechanics locked** 2026-07-19, **PP0-PP5 shipped** 2026-07-24 — dedicated table/panel, not Notes-backed; PP6 starter content still open); see `DECISIONS.md` |
| `docs/Name_Generator_Design.md` | Name generator v0.4 (gaps closed 2026-07-19, pre-implementation corrections 2026-07-22); NG0–NG7 **shipped** |
| `docs/Name_Generator_Region_Packs_Design.md` | Region pack catalog + install (**locked** 2026-07-23, **shipped in full** 2026-07-24/27); NP0-NP5 all done; hybrid core kept; offline packs at `E:\NameGeneratorPacks\` |
| `docs/Outline_Import_Design.md` | Import to Outline (**locked/shipped** 2026-07-20/24); OI0–OI8 done — see `DECISIONS.md`; parked Brainstorm/story-import note |
| `docs/Chat_Shuttle_Design.md` | Cross-desk chat shuttle (**locked** 2026-07-20); H0–H7 **shipped 2026-07-21** — see `DECISIONS.md` |
| `docs/Transfer_Log_And_Settings_IA_Design.md` | Settings IA + desk **send** transfer log (**locked/shipped** 2026-07-22); S0, T0–T3 done — see `DECISIONS.md` |
| `docs/Lorebook_Browse_Density_Design.md` | Lorebook Cards\|List toggle (**locked** 2026-07-21); L0–L3 — P2 B8, not started |
| `docs/Folders_Org_Design.md` | Cosmetic folders lore + chats (**locked/shipped** 2026-07-21); F0–F5 done — P2 B9; **T7 extends** engine to note artifacts (`kind: notes`) — see `Notes_Org_Browse_Design.md` |
| `docs/Notes_Org_Browse_Design.md` | T7 Notes desk org + browse UI (**locked/shipped** 2026-08-07/08); NO0–NO6 done — see `DECISIONS.md` |
| `docs/Context_Token_Meter_Design.md` | T4 context/token meter (**locked/shipped** 2026-07-22); M0–M5 done — see `DECISIONS.md` |
| `docs/Lexical_Editor_Design.md` | T2 Lexical deepen — **all 6 axes locked/shipped 2026-07-28, document fully closed.** LE0–LE3 (2026-07-21/22), CodeHighlight/TreeView/collab-residue, list "done" bar, toolbar/mobile/selection/rework polish (scoped subset), and upgrade breakage/compat matrix — see `DECISIONS.md` for each |
| `docs/Scene_Beat_Removal_Design.md` | Remove Scene Beats (**locked/shipped** 2026-07-21/22); keep Concrete Beats; SB0-SB8 done in full, including table drop (user-confirmed) — see `DECISIONS.md` |
| `docs/Chat_Model_Routing_And_Chrome_Design.md` | Chat Cloud\|Local defaults + collapsed context chrome + compact chat list (**locked/shipped** 2026-07-21/22); MR0-MR2/CC0/CL0 done — see `DECISIONS.md` |
| `docs/UI_Visual_Direction.md` | Linear A + Raycast chrome (**locked/shipped** 2026-07-21) |
| `docs/SL_Brand_And_Type.md` | Story Labyrinth brand + type kit (assets + guidance); Inter UI / Cormorant display / JetBrains Mono; wordmarks pinned 2026-08-04; **product rename SL0-SL3 shipped 2026-08-04** (user-promoted), **SL5 (DB filename/Docker/publish) shipped 2026-08-05**, SL4/SL6 still separate — see `DECISIONS.md` |
| `docs/SL_Brand_Type_Implement_Design.md` | **Claude implement plan** for brand/type (**locked 2026-08-04**); **BT0-BT4 all shipped 2026-08-04** (menus→Inter self-hosted; icons/wordmarks→`public/`; TopBar wordmark; brand color tokens + wordmark hover glow; Guide-page credit line) — see `DECISIONS.md` |
| `docs/Locations_And_Maps_Design.md` | Location playbooks, place sheet, Story Map L3 graph, image presets, place-Codex, multi-floor, export (**locked/shipped** L0-L5); product map **tool** direction → Maps v2 |
| `docs/Maps_V2_Sketch_Design.md` | Sketch maps Excalidraw (**MV0–MV7 shipped** 2026-08-07); kill graph Story Map UI — see `DECISIONS.md` |
| `docs/Story_Timeline_Design.md` | In-world Story Timeline (**TL0–TL12 all shipped** 2026-08-07); T6 — see `DECISIONS.md` |
| `docs/Local_Embeddings_Design.md` | Local in-process embeddings (**locked/shipped** 2026-07-22); IE0–IE6 done — see `DECISIONS.md` |
| `docs/Agent_Framework_And_Project_Memory_Design.md` | Agent A/B design (A/B shipped; C backlog) |
| `docs/Thin_Story_Graph_And_Lorebook_Visualization.md` | Graph design (G1 shipped; G1.5+ backlog) |
| Hermes `.hermes/plans/*` | Historical designs; may be stale vs CLAUDE |

---

*Update this file whenever a P0/P1 item ships or priority changes.*
